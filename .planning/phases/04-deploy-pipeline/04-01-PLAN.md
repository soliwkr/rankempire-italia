---
phase: 04-deploy-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - factory-core/src/db/schema.ts
  - factory-core/migrations/0004_add_deploy_columns.sql
  - factory-core/wrangler.toml
autonomous: true
requirements:
  - FACT-03
  - FACT-04
  - FACT-05

must_haves:
  truths:
    - "schema.ts ha le 3 colonne nullable (githubRepoUrl, pagesProjectName, pagesUrl) nella tabella projects"
    - "il file migration SQL esiste con le 3 ALTER TABLE in formato Drizzle Kit"
    - "wrangler.toml espone GITHUB_TEMPLATE_OWNER, GITHUB_TEMPLATE_REPO, FACTORY_API_URL in [vars] e commenta CF_API_TOKEN come secret"
  artifacts:
    - path: "factory-core/src/db/schema.ts"
      provides: "Drizzle schema con 3 nuovi campi su projects"
      contains: "githubRepoUrl"
    - path: "factory-core/migrations/0004_add_deploy_columns.sql"
      provides: "Migration SQL Drizzle Kit per D1"
      contains: "--> statement-breakpoint"
    - path: "factory-core/wrangler.toml"
      provides: "Env var configuration per deploy pipeline"
      contains: "GITHUB_TEMPLATE_OWNER"
  key_links:
    - from: "factory-core/migrations/0004_add_deploy_columns.sql"
      to: "factory-core/src/db/schema.ts"
      via: "Drizzle Kit migration applicata via wrangler d1 migrations apply"
      pattern: "ALTER TABLE.*projects.*ADD"
---

<objective>
Questo piano prepara l'infrastruttura dati e di configurazione per la deploy pipeline: aggiunge le 3 colonne di stato al D1 schema, crea il file migration SQL corrispondente, e inietta le variabili d'ambiente nel wrangler.toml.

Purpose: Nessun piano successivo (CloudflarePagesService, deploy endpoint) può funzionare senza il contratto dati corretto e le env var disponibili nelle Bindings.
Output: schema.ts aggiornato, migration SQL pronta, wrangler.toml con nuovi [vars].
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-deploy-pipeline/04-CONTEXT.md
@.planning/phases/04-deploy-pipeline/04-PATTERNS.md

<interfaces>
<!-- Drizzle schema attuale — factory-core/src/db/schema.ts linee 13-24 -->
```typescript
export const projects = sqliteTable('projects', {
  id: text('id').primaryKey(),
  slug: text('slug').notNull().unique(),
  name: text('name').notNull(),
  niche: text('niche').notNull(),
  location: text('location').notNull(),
  domain: text('domain'),           // nullable — pattern da replicare
  status: text('status').default('pending').notNull(),
  renterId: text('renter_id').references(() => renters.id),
  configJson: text('config_json'),  // nullable — pattern da replicare
  createdAt: text('created_at').default(sql`CURRENT_TIMESTAMP`).notNull(),
});
```

<!-- Formato migration Drizzle Kit (0003_petite_jack_murdock.sql) -->
```sql
ALTER TABLE `leads` ADD `email` text;
```
<!-- Nota: nessun statement-breakpoint su singola riga — ma le multi-riga usano il separatore -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere 3 colonne nullable a schema.ts</name>
  <files>factory-core/src/db/schema.ts</files>
  <read_first>
    - factory-core/src/db/schema.ts (leggere l'intera tabella projects per vedere il punto esatto di inserimento e la convenzione camelCase/snake_case)
  </read_first>
  <action>
    Aprire factory-core/src/db/schema.ts. Nella definizione di `projects` (sqliteTable), aggiungere dopo la riga `configJson: text('config_json'),` le seguenti 3 colonne nullable — rispettando la convenzione camelCase TypeScript / snake_case SQL già presente in tutto lo schema (es. `renterId` → `renter_id`, `configJson` → `config_json`):

    ```typescript
    githubRepoUrl: text('github_repo_url'),
    pagesProjectName: text('pages_project_name'),
    pagesUrl: text('pages_url'),
    ```

    Queste sono nullable (nessun `.notNull()`) — identico pattern di `domain` e `configJson`. NON modificare nessun altro campo. NON aggiungere import. NON riordinare le colonne esistenti.
  </action>
  <verify>
    <automated>grep -c 'githubRepoUrl\|pagesProjectName\|pagesUrl' /home/soliwkr/Work/Codice/rankempire-italia/factory-core/src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'githubRepoUrl' factory-core/src/db/schema.ts` restituisce `githubRepoUrl: text('github_repo_url'),`
    - `grep 'pagesProjectName' factory-core/src/db/schema.ts` restituisce `pagesProjectName: text('pages_project_name'),`
    - `grep 'pagesUrl' factory-core/src/db/schema.ts` restituisce `pagesUrl: text('pages_url'),`
    - Il file compila senza errori: `cd factory-core && npx wrangler deploy --dry-run 2>&1 | grep -i error` non produce output TypeScript error
  </acceptance_criteria>
  <done>Le 3 colonne nullable esistono in schema.ts nella tabella projects con naming convention corretto.</done>
</task>

<task type="auto">
  <name>Task 2: Creare migration SQL 0004_add_deploy_columns.sql</name>
  <files>factory-core/migrations/0004_add_deploy_columns.sql</files>
  <read_first>
    - factory-core/migrations/0003_petite_jack_murdock.sql (per vedere il formato esatto Drizzle Kit con o senza statement-breakpoint)
    - factory-core/migrations/ (listare i file per confermare il numero sequenziale corretto — il prossimo deve essere 0004)
  </read_first>
  <action>
    Creare il file `factory-core/migrations/0004_add_deploy_columns.sql` con il seguente contenuto esatto (3 ALTER TABLE con il separatore `--> statement-breakpoint` Drizzle Kit tra ogni coppia di statement):

    ```sql
    ALTER TABLE `projects` ADD `github_repo_url` text;--> statement-breakpoint
    ALTER TABLE `projects` ADD `pages_project_name` text;--> statement-breakpoint
    ALTER TABLE `projects` ADD `pages_url` text;
    ```

    Il formato con `--> statement-breakpoint` alla fine della riga (non su riga separata) è il formato Drizzle Kit standard — copiare esattamente questo stile. Le colonne sono `text` nullable (nessun `NOT NULL`).
  </action>
  <verify>
    <automated>cat /home/soliwkr/Work/Codice/rankempire-italia/factory-core/migrations/0004_add_deploy_columns.sql</automated>
  </verify>
  <acceptance_criteria>
    - Il file `factory-core/migrations/0004_add_deploy_columns.sql` esiste
    - `grep 'github_repo_url' factory-core/migrations/0004_add_deploy_columns.sql` restituisce `ALTER TABLE \`projects\` ADD \`github_repo_url\` text;--> statement-breakpoint`
    - `grep 'pages_project_name' factory-core/migrations/0004_add_deploy_columns.sql` restituisce la riga con statement-breakpoint
    - `grep 'pages_url' factory-core/migrations/0004_add_deploy_columns.sql` restituisce `ALTER TABLE \`projects\` ADD \`pages_url\` text;` (ultima riga, senza statement-breakpoint)
    - Il file contiene esattamente 3 righe (nessuna riga vuota extra)
  </acceptance_criteria>
  <done>Migration SQL pronta nel formato Drizzle Kit corretto, con 3 ALTER TABLE su tabella projects.</done>
</task>

<task type="auto">
  <name>Task 3: Aggiungere env var deploy pipeline a wrangler.toml</name>
  <files>factory-core/wrangler.toml</files>
  <read_first>
    - factory-core/wrangler.toml (per vedere il blocco [vars] corrente e preservare la formattazione e lo stile commento esistente)
  </read_first>
  <action>
    Aprire factory-core/wrangler.toml. Nel blocco `[vars]`, aggiungere le seguenti variabili DOPO la riga `VERIFICATION_BASE_URL`:

    ```toml
    GITHUB_TEMPLATE_OWNER = "StudioPuraLuce"
    GITHUB_TEMPLATE_REPO = "astro-rank-rent"
    FACTORY_API_URL = "https://factory-core.soliwkr.workers.dev"
    # CF_API_TOKEN is secret, set via .dev.vars or 'wrangler secret put'
    ```

    Anche aggiungere `CF_API_TOKEN` e `GITHUB_TEMPLATE_OWNER`/`GITHUB_TEMPLATE_REPO` a factory-core/.dev.vars (se il file esiste) per sviluppo locale — solo `CF_API_TOKEN` è sensibile e NON va in [vars]. NON aggiungere CF_API_TOKEN a [vars]. Mantenere lo stesso stile di commento già usato per RESEND_API_KEY e API_SECRET.
  </action>
  <verify>
    <automated>grep -c 'GITHUB_TEMPLATE_OWNER\|GITHUB_TEMPLATE_REPO\|FACTORY_API_URL' /home/soliwkr/Work/Codice/rankempire-italia/factory-core/wrangler.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'GITHUB_TEMPLATE_OWNER' factory-core/wrangler.toml` restituisce `GITHUB_TEMPLATE_OWNER = "StudioPuraLuce"`
    - `grep 'GITHUB_TEMPLATE_REPO' factory-core/wrangler.toml` restituisce `GITHUB_TEMPLATE_REPO = "astro-rank-rent"`
    - `grep 'FACTORY_API_URL' factory-core/wrangler.toml` restituisce `FACTORY_API_URL = "https://factory-core.soliwkr.workers.dev"`
    - `grep 'CF_API_TOKEN' factory-core/wrangler.toml` restituisce solo la riga commento (inizia con `#`), NON una riga `CF_API_TOKEN = "..."` — il token è secret, mai in chiaro in [vars]
    - `grep 'CF_API_TOKEN' factory-core/wrangler.toml | grep -v '^#'` non restituisce output (zero risultati non-commento)
  </acceptance_criteria>
  <done>wrangler.toml ha le 3 env var non-sensitive in [vars] e CF_API_TOKEN documentato solo come commento secret.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrangler.toml → deployment | Valori in [vars] sono leggibili da chiunque abbia accesso al repo |
| .dev.vars → local dev | File locale con secrets reali — non deve mai essere committato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | wrangler.toml [vars] | mitigate | CF_API_TOKEN NON va in [vars] — solo commento `# set via wrangler secret put`. Verificato in acceptance_criteria con grep gate |
| T-04-01-02 | Information Disclosure | factory-core/.dev.vars | accept | .dev.vars è in .gitignore per convenzione Cloudflare Workers — confermato che non viene committato. Secrets locali per sviluppo, non produzione |
| T-04-01-03 | Tampering | factory-core/migrations/0004_add_deploy_columns.sql | accept | Migration è idempotente via Drizzle Kit tracking — se applicata due volte, wrangler la skippa automaticamente |
</threat_model>

<verification>
Verifiche post-esecuzione:

```bash
# 1. Schema ha le 3 nuove colonne
grep 'githubRepoUrl\|pagesProjectName\|pagesUrl' factory-core/src/db/schema.ts

# 2. Migration SQL esiste con formato corretto
cat factory-core/migrations/0004_add_deploy_columns.sql

# 3. wrangler.toml ha vars corrette, senza CF_API_TOKEN in chiaro
grep -v '^#' factory-core/wrangler.toml | grep 'CF_API_TOKEN' && echo "FAIL: token in vars" || echo "OK: no token in vars"

# 4. TypeScript compila
cd factory-core && npx wrangler deploy --dry-run 2>&1 | tail -5
```
</verification>

<success_criteria>
- schema.ts: tabella `projects` ha 3 nuove colonne nullable (githubRepoUrl, pagesProjectName, pagesUrl) con naming convention snake_case SQL / camelCase TS
- migration: `factory-core/migrations/0004_add_deploy_columns.sql` esiste con 3 ALTER TABLE in formato Drizzle Kit
- config: `wrangler.toml` ha GITHUB_TEMPLATE_OWNER, GITHUB_TEMPLATE_REPO, FACTORY_API_URL in [vars]; CF_API_TOKEN solo in commento
- TypeScript: `npx wrangler deploy --dry-run` esce con codice 0
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-deploy-pipeline/04-01-SUMMARY.md`
</output>
