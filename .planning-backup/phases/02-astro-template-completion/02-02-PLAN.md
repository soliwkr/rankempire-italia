---
phase: 02-astro-template-completion
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - rankame/templates/astro-base/vitest.config.ts
  - rankame/templates/astro-base/tests/fixtures/site.config.json
  - rankame/templates/astro-base/tests/fetch-pages.test.ts
  - rankame/templates/astro-base/tests/schema-graph.test.ts
  - rankame/templates/astro-base/tests/routing.test.ts
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-04
must_haves:
  truths:
    - "I test unitari per fetchPages girano in meno di 10 secondi"
    - "I test verificano che requirePage() lanci un errore chiaro su slug mancante"
    - "I test verificano che SchemaGraph ometta i campi null in ghost mode"
    - "I test verificano che il grafo @graph contenga il nodo Service su pagine service_zone"
    - "I test verificano che getStaticPaths() enumeri routes da services × zones"
  artifacts:
    - path: "rankame/templates/astro-base/vitest.config.ts"
      provides: "Configurazione vitest per progetto Astro"
    - path: "rankame/templates/astro-base/tests/fixtures/site.config.json"
      provides: "Mock config con dati esempio italiani"
    - path: "rankame/templates/astro-base/tests/fetch-pages.test.ts"
      provides: "Test per fetchAllPages e requirePage (SITE-04)"
    - path: "rankame/templates/astro-base/tests/schema-graph.test.ts"
      provides: "Test per logica SchemaGraph null-omission e @graph (SITE-02)"
    - path: "rankame/templates/astro-base/tests/routing.test.ts"
      provides: "Test per enumerazione route services × zones (SITE-01)"
  key_links:
    - from: "rankame/templates/astro-base/tests/*.test.ts"
      to: "rankame/templates/astro-base/src/lib/fetchPages.ts"
      via: "import testato indirettamente tramite logica estratta"
      pattern: "fetch-pages\\.test"
---

<objective>
Creare l'infrastruttura di test (vitest config + 3 file di test + fixture) che copre la logica critica dei requirements SITE-01, SITE-02, SITE-04 — prima che il codice di produzione esista.

Purpose: I test devono essere scritti PRIMA del codice (TDD). Quando il Piano 03 implementa `fetchPages.ts` e `SchemaGraph.astro`, i test diventano verdi. Senza questa infrastruttura, la Fase 2 non ha feedback automatico.
Output: 3 file di test rossi, 1 vitest.config.ts, 1 fixture.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-astro-template-completion/02-VALIDATION.md
@.planning/phases/02-astro-template-completion/02-RESEARCH.md

<interfaces>
<!-- Contratti che i test devono verificare — non ancora implementati. -->

Interfacce attese in src/lib/fetchPages.ts (da creare in Piano 03):
```typescript
export interface PageContent {
  id: string;
  projectId: string;
  slug: string;
  type: 'homepage' | 'service' | 'zone' | 'service_zone' | 'blog';
  title: string;
  body: string;
  faq: Array<{ question: string; answer: string }>;
  meta: { description: string; canonical: string };
}

export async function fetchAllPages(): Promise<PageContent[]>
export function requirePage(pages: PageContent[], slug: string): PageContent
// requirePage() DEVE lanciare Error se slug non trovato
```

Logica attesa in SchemaGraph (da testare come funzione pura estratta):
```typescript
// Dato siteConfig ghost (businessName: null, phone: null, address: null, geo: null)
// il JSON-LD NON deve contenere "name", "telephone", "address", "geo"
// il JSON-LD DEVE contenere "@context", "@graph" array
// su pageType "service_zone" il @graph DEVE contenere un nodo { "@type": "Service" }
```

Logica routing attesa (da testare):
```typescript
// Dato siteConfig.services = ["ristrutturazioni-interni", "rifacimento-bagno"]
// Dato siteConfig.zones = ["formia", "gaeta", "minturno"]
// getStaticPaths deve produrre 6 route (2×3), ognuna con params.service e params.zone come stringhe
```
</interfaces>
</context>

<feature>
  <name>Test infrastructure per Phase 2</name>
  <files>
    rankame/templates/astro-base/vitest.config.ts,
    rankame/templates/astro-base/tests/fixtures/site.config.json,
    rankame/templates/astro-base/tests/fetch-pages.test.ts,
    rankame/templates/astro-base/tests/schema-graph.test.ts,
    rankame/templates/astro-base/tests/routing.test.ts
  </files>
  <behavior>
    SITE-04 (fetchPages):
    - fetchAllPages() chiama fetch con URL `${factoryApi}/api/sites/${projectId}/pages`
    - requirePage() ritorna la pagina quando lo slug esiste
    - requirePage() lancia Error con messaggio che include lo slug quando non trovato
    - il messaggio di errore contiene la stringa "Phase 3" (hint diagnostico per lo sviluppatore)

    SITE-02 (SchemaGraph):
    - ghost mode (tutti i campi null): il JSON-LD NON contiene "name", "telephone", "address", "geo"
    - ghost mode: il JSON-LD CONTIENE "@context" e "@graph" come array
    - ghost mode: il @graph CONTIENE un nodo con "@type": "LocalBusiness" (o sottotipo)
    - service_zone: il @graph CONTIENE un nodo con "@type": "Service"
    - homepage: il @graph CONTIENE un nodo con "@type": "FAQPage" quando faqs non è vuoto
    - ogni pagina: il @graph CONTIENE un nodo con "@type": "BreadcrumbList"

    SITE-01 (routing):
    - la funzione di enumeration paths restituisce services.length × zones.length paths
    - ogni path ha params.service e params.zone come stringhe (non numeri, non oggetti — Astro 6 requisito)
    - la funzione lancia Error quando un slug mancante viene richiesto via requirePage
  </behavior>
  <implementation>
    Dopo che questi test sono rossi (RED), il Piano 03 crea il codice di produzione che li fa diventare verdi (GREEN).

    Per testare SchemaGraph (un componente Astro — non importabile direttamente in vitest):
    Estrarre la logica di costruzione del grafo in una funzione TypeScript pura `buildSchemaGraph(config, props): object`
    da `src/lib/buildSchemaGraph.ts`. Il componente `SchemaGraph.astro` chiamerà questa funzione.
    Il test importa e testa `buildSchemaGraph` direttamente — nessun runtime Astro necessario.

    Per testare routing:
    Estrarre la funzione di enumerazione in `src/lib/buildPaths.ts` che prende `config.services[]` e `config.zones[]`
    e restituisce `Array<{ params: { service: string; zone: string } }>`. Le pagine la chiamano in getStaticPaths().
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: vitest.config.ts + fixture site.config.json</name>

  <read_first>
    - rankame/templates/astro-base/package.json — verificare versione vitest installata e dipendenze disponibili
  </read_first>

  <files>
    rankame/templates/astro-base/vitest.config.ts,
    rankame/templates/astro-base/tests/fixtures/site.config.json
  </files>

  <action>
1. **Crea rankame/templates/astro-base/vitest.config.ts**:
   ```typescript
   import { defineConfig } from 'vitest/config';

   export default defineConfig({
     test: {
       environment: 'node',
       include: ['tests/**/*.test.ts'],
       globals: false,
     },
   });
   ```
   Usare environment `node` — nessun DOM necessario. I test verificano logica pura TypeScript.

2. **Crea rankame/templates/astro-base/tests/fixtures/site.config.json**:
   ```json
   {
     "projectId": "test-project-001",
     "niche": "ristrutturazioni",
     "city": "formia",
     "services": ["ristrutturazioni-interni", "rifacimento-bagno"],
     "zones": ["formia", "gaeta", "minturno"],
     "avatar": "in-pain",
     "factoryApi": "https://factory-core.soliwkr.workers.dev",
     "slug": "ristrutturazioni-formia",
     "domain": "ristrutturazioniformia.it",
     "gmbPlaceId": null,
     "ga4MeasurementId": null,
     "businessName": null,
     "phone": null,
     "address": null,
     "geo": null
   }
   ```
   Questo JSON rappresenta esattamente il formato D-01 in ghost mode (tutti i campi opzionali a null).
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base && cat vitest.config.ts && cat tests/fixtures/site.config.json</automated>
  </verify>

  <acceptance_criteria>
    - `rankame/templates/astro-base/vitest.config.ts` esiste con `environment: 'node'` e `include: ['tests/**/*.test.ts']`
    - `rankame/templates/astro-base/tests/fixtures/site.config.json` ha tutti i campi D-01: projectId, niche, city, services (array 2), zones (array 3), avatar, factoryApi, slug, domain, e tutti i campi nullable a null
  </acceptance_criteria>

  <done>vitest.config.ts e fixture creati</done>
</task>

<task type="tdd">
  <name>Task 2: Test RED per fetchPages, schemaGraph, routing</name>

  <read_first>
    - rankame/templates/astro-base/tests/fixtures/site.config.json — usare la fixture appena creata per i test
    - rankame/templates/astro-base/package.json — confermare `vitest` presente nelle devDependencies
  </read_first>

  <files>
    rankame/templates/astro-base/tests/fetch-pages.test.ts,
    rankame/templates/astro-base/tests/schema-graph.test.ts,
    rankame/templates/astro-base/tests/routing.test.ts
  </files>

  <action>
1. **Crea rankame/templates/astro-base/tests/fetch-pages.test.ts**:
   ```typescript
   import { describe, it, expect, vi, beforeEach } from 'vitest';

   // Questi import falliranno finché Piano 03 non crea i file — comportamento atteso (RED)
   // @ts-expect-error — file non ancora creato
   import { fetchAllPages, requirePage, type PageContent } from '../src/lib/fetchPages';

   const mockPages: PageContent[] = [
     {
       id: '1', projectId: 'test-project-001', slug: 'homepage', type: 'homepage',
       title: 'Ristrutturazioni Formia', body: '<p>Test</p>',
       faq: [{ question: 'Q?', answer: 'A.' }],
       meta: { description: 'Meta test', canonical: 'https://ristrutturazioniformia.it/' }
     },
     {
       id: '2', projectId: 'test-project-001', slug: 'ristrutturazioni-interni/formia',
       type: 'service_zone', title: 'Ristrutturazioni Formia Interni', body: '<p>Test</p>',
       faq: [], meta: { description: 'Meta', canonical: 'https://ristrutturazioniformia.it/ristrutturazioni-interni/formia/' }
     }
   ];

   describe('fetchAllPages', () => {
     beforeEach(() => {
       vi.resetAllMocks();
     });

     it('chiama fetch con URL corretto (factoryApi + projectId)', async () => {
       const fetchMock = vi.fn().mockResolvedValue({
         ok: true,
         json: async () => ({ pages: mockPages })
       });
       vi.stubGlobal('fetch', fetchMock);

       await fetchAllPages();

       expect(fetchMock).toHaveBeenCalledWith(
         expect.stringContaining('/api/sites/test-project-001/pages')
       );
     });

     it('lancia Error con status code se risposta non ok', async () => {
       vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: false, status: 503 }));

       await expect(fetchAllPages()).rejects.toThrow('503');
     });
   });

   describe('requirePage', () => {
     it('restituisce la pagina quando lo slug esiste', () => {
       const page = requirePage(mockPages, 'homepage');
       expect(page.slug).toBe('homepage');
     });

     it('lancia Error con messaggio che include lo slug mancante', () => {
       expect(() => requirePage(mockPages, 'slug-inesistente')).toThrow('slug-inesistente');
     });

     it('il messaggio di errore include "Phase 3" come hint diagnostico', () => {
       expect(() => requirePage(mockPages, 'slug-inesistente')).toThrow('Phase 3');
     });
   });
   ```

2. **Crea rankame/templates/astro-base/tests/schema-graph.test.ts**:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import siteConfigFixture from './fixtures/site.config.json';

   // @ts-expect-error — file non ancora creato (Piano 03)
   import { buildSchemaGraph } from '../src/lib/buildSchemaGraph';

   describe('buildSchemaGraph — ghost mode (tutti i campi null)', () => {
     const config = siteConfigFixture; // businessName, phone, address, geo sono null

     it('produce un oggetto con @context e @graph array', () => {
       const schema = buildSchemaGraph(config, { pageType: 'homepage', breadcrumbs: [{ name: 'Home', url: 'https://ristrutturazioniformia.it/' }], faqs: [] });
       expect(schema['@context']).toBe('https://schema.org');
       expect(Array.isArray(schema['@graph'])).toBe(true);
     });

     it('omette "name" dal nodo LocalBusiness quando businessName è null', () => {
       const schema = buildSchemaGraph(config, { pageType: 'homepage', breadcrumbs: [], faqs: [] });
       const lb = schema['@graph'].find((n: any) => n['@type'] === 'LocalBusiness' || n['@type'] === 'HomeAndConstructionBusiness');
       expect(lb).toBeDefined();
       expect(lb.name).toBeUndefined();
     });

     it('omette "telephone" quando phone è null', () => {
       const schema = buildSchemaGraph(config, { pageType: 'homepage', breadcrumbs: [], faqs: [] });
       const lb = schema['@graph'].find((n: any) => n.url);
       expect(lb.telephone).toBeUndefined();
     });

     it('contiene BreadcrumbList su ogni pageType', () => {
       const schema = buildSchemaGraph(config, {
         pageType: 'service_zone',
         serviceSlug: 'ristrutturazioni-interni',
         zoneSlug: 'formia',
         breadcrumbs: [{ name: 'Home', url: 'https://ristrutturazioniformia.it/' }],
         faqs: []
       });
       const bc = schema['@graph'].find((n: any) => n['@type'] === 'BreadcrumbList');
       expect(bc).toBeDefined();
       expect(bc.itemListElement).toHaveLength(1);
     });

     it('contiene nodo Service su pageType service_zone', () => {
       const schema = buildSchemaGraph(config, {
         pageType: 'service_zone',
         serviceSlug: 'ristrutturazioni-interni',
         zoneSlug: 'formia',
         breadcrumbs: [],
         faqs: []
       });
       const service = schema['@graph'].find((n: any) => n['@type'] === 'Service');
       expect(service).toBeDefined();
       expect(service.areaServed).toMatchObject({ '@type': 'City', name: 'formia' });
     });

     it('contiene FAQPage su homepage quando faqs non è vuoto', () => {
       const schema = buildSchemaGraph(config, {
         pageType: 'homepage',
         breadcrumbs: [],
         faqs: [{ question: 'Domanda?', answer: 'Risposta.' }]
       });
       const faq = schema['@graph'].find((n: any) => n['@type'] === 'FAQPage');
       expect(faq).toBeDefined();
       expect(faq.mainEntity).toHaveLength(1);
     });

     it('NON include FAQPage su homepage quando faqs è vuoto', () => {
       const schema = buildSchemaGraph(config, { pageType: 'homepage', breadcrumbs: [], faqs: [] });
       const faq = schema['@graph'].find((n: any) => n['@type'] === 'FAQPage');
       expect(faq).toBeUndefined();
     });
   });
   ```

3. **Crea rankame/templates/astro-base/tests/routing.test.ts**:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import siteConfigFixture from './fixtures/site.config.json';

   // @ts-expect-error — file non ancora creato (Piano 03)
   import { buildServiceZonePaths } from '../src/lib/buildPaths';

   describe('buildServiceZonePaths', () => {
     it('produce services.length × zones.length paths', () => {
       const paths = buildServiceZonePaths(siteConfigFixture.services, siteConfigFixture.zones, []);
       expect(paths).toHaveLength(siteConfigFixture.services.length * siteConfigFixture.zones.length);
     });

     it('ogni path ha params.service e params.zone come stringhe', () => {
       const mockPages = siteConfigFixture.services.flatMap(s =>
         siteConfigFixture.zones.map(z => ({
           id: `${s}-${z}`, projectId: 'test', slug: `${s}/${z}`,
           type: 'service_zone' as const, title: '', body: '', faq: [],
           meta: { description: '', canonical: '' }
         }))
       );
       const paths = buildServiceZonePaths(siteConfigFixture.services, siteConfigFixture.zones, mockPages);
       paths.forEach(p => {
         expect(typeof p.params.service).toBe('string');
         expect(typeof p.params.zone).toBe('string');
       });
     });

     it('lancia Error quando il contenuto per uno slug è mancante', () => {
       expect(() => buildServiceZonePaths(siteConfigFixture.services, siteConfigFixture.zones, [])).toThrow();
     });
   });
   ```

**Stato atteso dopo questo task:** tutti e tre i test file esistono, `npx vitest run` fallisce con errori di import (file non trovati) — comportamento corretto TDD RED.
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base && npx vitest run 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - I 3 file di test esistono in `tests/`
    - `npx vitest run` fallisce (exit code non-zero) con errori di tipo "Cannot find module" o simili — NON con errori di sintassi TypeScript nei test stessi
    - La fixture `tests/fixtures/site.config.json` è importabile senza errori
  </acceptance_criteria>

  <done>3 test file rossi (RED) — import fallisce perché il codice di produzione non esiste ancora</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | I test non hanno trust boundary rilevanti — sono eseguiti solo in ambiente di sviluppo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | tests/fixtures/site.config.json | accept | Contiene solo dati di test placeholder — nessun secret, nessun projectId di produzione. File committed in repo è sicuro. |
</threat_model>

<verification>
1. `cd rankame/templates/astro-base && ls tests/` → mostra fetch-pages.test.ts, schema-graph.test.ts, routing.test.ts, fixtures/
2. `cd rankame/templates/astro-base && ls tests/fixtures/` → mostra site.config.json
3. `cd rankame/templates/astro-base && cat vitest.config.ts | grep environment` → mostra `environment: 'node'`
4. `cd rankame/templates/astro-base && npx vitest run 2>&1 | grep -E "FAIL|Error"` → mostra errori (RED — atteso)
</verification>

<success_criteria>
- vitest.config.ts esiste con configurazione node
- Fixture site.config.json ha tutti i campi D-01 con valori di test italiani
- 3 test file esistono con test che coprono SITE-01, SITE-02, SITE-04
- I test falliscono per "module not found" — non per errori di sintassi nel codice di test
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-astro-template-completion/02-02-SUMMARY.md`
</output>
