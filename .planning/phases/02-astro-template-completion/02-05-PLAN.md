---
phase: 02-astro-template-completion
plan: 05
type: execute
wave: 4
depends_on:
  - "02-04"
files_modified:
  - rankame/templates/astro-base/src/pages/robots.txt.ts
autonomous: false
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
must_haves:
  truths:
    - "robots.txt è generato dinamicamente con URL sitemap corretto dal dominio reale"
    - "sitemap-index.xml è presente nell'output di build con almeno homepage + service + zone pages"
    - "npx astro build completa senza errori con dati mock"
    - "Il numero di URL in sitemap corrisponde alla somma delle pagine generate (homepage + services + zones + service×zones + blog index)"
  artifacts:
    - path: "rankame/templates/astro-base/src/pages/robots.txt.ts"
      provides: "Endpoint robots.txt con URL sitemap dinamico"
    - path: "rankame/templates/astro-base/dist/sitemap-index.xml"
      provides: "Sitemap XML generata al build time"
    - path: "rankame/templates/astro-base/dist/robots.txt"
      provides: "robots.txt con riferimento alla sitemap"
  key_links:
    - from: "rankame/templates/astro-base/src/pages/robots.txt.ts"
      to: "rankame/templates/astro-base/astro.config.mjs"
      via: "Astro.site → valore di site: in astro.config.mjs"
      pattern: "robots\\.txt\\.ts"
    - from: "rankame/templates/astro-base/astro.config.mjs"
      to: "@astrojs/sitemap"
      via: "integrations: [sitemap()]"
      pattern: "sitemap"
---

<objective>
Completare la pipeline di build: creare robots.txt dinamico, verificare che @astrojs/sitemap generi sitemap-index.xml correttamente, eseguire la build smoke test con mock server, e verificare checkpoint visivo sui file di output.

Purpose: Questo è il gate finale della Fase 2. Se la build gira e sitemap + robots.txt sono presenti, tutti e 4 i success criteria del ROADMAP per questa fase sono soddisfatti.
Output: robots.txt.ts, build smoke test verde, checkpoint visivo confermato dall'utente.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-astro-template-completion/02-CONTEXT.md
@.planning/phases/02-astro-template-completion/02-RESEARCH.md
@.planning/phases/02-astro-template-completion/02-04-SUMMARY.md

<interfaces>
<!-- Contratto robots.txt.ts da RESEARCH.md §Pattern 5 (verificato da docs ufficiali Astro): -->
```typescript
import type { APIRoute } from 'astro';

export const GET: APIRoute = ({ site }) => {
  const sitemapURL = new URL('sitemap-index.xml', site);
  return new Response(`User-agent: *\nAllow: /\n\nSitemap: ${sitemapURL.href}`);
};
// `site` viene automaticamente da astro.config.mjs → site: "https://${siteConfig.domain}"
```

Struttura build output attesa:
```
dist/
  index.html                          ← homepage
  [service]/index.html × N            ← service hub pages
  [service]/[zone]/index.html × N×M   ← leaf pages
  zone/[zone]/index.html × M          ← zone hub pages
  blog/index.html                     ← blog index
  robots.txt                          ← generato da robots.txt.ts
  sitemap-index.xml                   ← generato da @astrojs/sitemap
  sitemap-0.xml                       ← contenuto sitemap
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: robots.txt.ts + mock server + build smoke test</name>

  <read_first>
    - rankame/templates/astro-base/astro.config.mjs — verificare che `site:` sia dinamico e `integrations: [sitemap()]` sia presente (dal Piano 03)
    - rankame/templates/astro-base/src/data/site.config.json — verificare che il file esista con domain e factoryApi valorizzati
    - factory-core/src/index.ts — il mock server risponderà allo stesso endpoint dell'app reale
  </read_first>

  <files>
    rankame/templates/astro-base/src/pages/robots.txt.ts
  </files>

  <action>
1. **Crea rankame/templates/astro-base/src/pages/robots.txt.ts**:
   ```typescript
   import type { APIRoute } from 'astro';

   export const GET: APIRoute = ({ site }) => {
     const sitemapURL = new URL('sitemap-index.xml', site);
     return new Response(`User-agent: *\nAllow: /\n\nSitemap: ${sitemapURL.href}`);
   };
   ```
   CRITICO: usare `site` da Astro.locals — non hardcodare l'URL della sitemap. Questo valore viene da `astro.config.mjs → site: "https://${siteConfig.domain}"`.

2. **Eseguire la build smoke test con mock server**:
   Poiché Phase 3 non è ancora eseguita (D1 non ha contenuto), avviare un mock server locale che risponde all'endpoint `GET /api/sites/:projectId/pages` con dati di test:

   Creare file temporaneo `rankame/templates/astro-base/scripts/mock-factory.cjs` per il server mock:
   ```javascript
   const http = require('http');

   const MOCK_PAGES = [
     { id: '1', projectId: 'dev-ristrutturazioni-formia', slug: 'homepage', type: 'homepage',
       title: 'Ristrutturazioni Formia — Preventivi Gratuiti', body: '<h1>Ristrutturazioni a Formia</h1><p>Il tuo esperto locale.</p>',
       faq: [{ question: 'Quanto costa?', answer: 'Dipende dal progetto.' }],
       meta: { description: 'Ristrutturazioni Formia, prezzi onesti.', canonical: 'https://ristrutturazioniformia.it/' } },
   ];

   // Genera pagine service, zone, service_zone per i valori della fixture
   const services = ['ristrutturazioni-interni', 'rifacimento-bagno', 'ristrutturazioni-esterni'];
   const zones = ['formia', 'gaeta', 'minturno', 'fondi', 'terracina', 'sperlonga', 'itri', 'cassino', 'frosinone'];

   services.forEach(s => {
     MOCK_PAGES.push({ id: `s-${s}`, projectId: 'dev-ristrutturazioni-formia', slug: s, type: 'service',
       title: `${s.replace(/-/g, ' ')} a Formia`, body: `<h1>${s.replace(/-/g, ' ')}</h1>`,
       faq: [], meta: { description: `${s} Formia`, canonical: `https://ristrutturazioniformia.it/${s}/` } });
   });

   zones.forEach(z => {
     MOCK_PAGES.push({ id: `z-${z}`, projectId: 'dev-ristrutturazioni-formia', slug: `zone/${z}`, type: 'zone',
       title: `Ristrutturazioni a ${z}`, body: `<h1>Ristrutturazioni ${z}</h1>`,
       faq: [], meta: { description: `Ristrutturazioni ${z}`, canonical: `https://ristrutturazioniformia.it/zone/${z}/` } });
   });

   services.forEach(s => {
     zones.forEach(z => {
       MOCK_PAGES.push({ id: `sz-${s}-${z}`, projectId: 'dev-ristrutturazioni-formia', slug: `${s}/${z}`, type: 'service_zone',
         title: `${s.replace(/-/g, ' ')} a ${z}`, body: `<h1>${s.replace(/-/g, ' ')} a ${z}</h1>`,
         faq: [{ question: `Come scegliere un esperto di ${s} a ${z}?`, answer: 'Verifica esperienza e preventivi.' }],
         meta: { description: `${s} ${z}`, canonical: `https://ristrutturazioniformia.it/${s}/${z}/` } });
     });
   });

   const server = http.createServer((req, res) => {
     res.setHeader('Access-Control-Allow-Origin', '*');
     res.setHeader('Content-Type', 'application/json');
     if (req.url && req.url.includes('/api/sites/') && req.url.endsWith('/pages')) {
       res.writeHead(200);
       res.end(JSON.stringify({ pages: MOCK_PAGES }));
     } else {
       res.writeHead(404);
       res.end(JSON.stringify({ error: 'Not found' }));
     }
   });

   server.listen(3001, () => console.log('Mock factory-core running on http://localhost:3001'));
   ```

   Modificare temporaneamente `src/data/site.config.json` per usare il mock durante la build:
   - Cambiare `"factoryApi": "http://localhost:3001"` (temporaneo — solo per smoke test)

   Eseguire la build:
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base
   node scripts/mock-factory.cjs &
   MOCK_PID=$!
   npx astro build
   BUILD_EXIT=$?
   kill $MOCK_PID 2>/dev/null
   exit $BUILD_EXIT
   ```

   Dopo la build, ripristinare `factoryApi` al valore di produzione:
   - `"factoryApi": "https://factory-core.soliwkr.workers.dev"`

   Verificare l'output:
   ```bash
   ls dist/sitemap-index.xml dist/robots.txt
   grep "sitemap" dist/robots.txt
   grep -c "<url>" dist/sitemap-0.xml 2>/dev/null || grep -c "loc" dist/sitemap-0.xml
   ```

   Numero URL attesi in sitemap: 1 (homepage) + 3 (service hub) + 9 (zone hub) + 27 (service×zone) + 1 (blog index) = **41 URL**
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base && ls dist/sitemap-index.xml dist/robots.txt 2>&1 && grep "sitemap" dist/robots.txt 2>&1 | head -3</automated>
  </verify>

  <acceptance_criteria>
    - `rankame/templates/astro-base/src/pages/robots.txt.ts` esiste con export GET e `new URL('sitemap-index.xml', site)`
    - `npx astro build` → exit code 0 (zero errori)
    - `ls dist/sitemap-index.xml` → file esiste
    - `ls dist/robots.txt` → file esiste
    - `grep "Sitemap:" dist/robots.txt` → 1 match con URL che contiene "sitemap-index.xml"
    - `grep -c "loc>" dist/sitemap-0.xml` → almeno 40 URL (homepage + services + zones + service×zones + blog index)
    - `grep "example.com" dist/sitemap-0.xml` → 0 match (il dominio reale è nella sitemap, non example.com)
    - `factoryApi` in src/data/site.config.json ripristinato a `"https://factory-core.soliwkr.workers.dev"` dopo la build
  </acceptance_criteria>

  <done>robots.txt.ts creato, build smoke test verde, sitemap e robots.txt nell'output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    La build completa del template Astro ha prodotto:
    - Homepage + service hub (3 pagine) + zone hub (9 pagine) + leaf service×zone (27 pagine) + blog index = 41 pagine statiche
    - sitemap-index.xml con 41+ URL
    - robots.txt con URL sitemap corretto
    - Schema.org @graph in ogni pagina (LocalBusiness, BreadcrumbList, Service su leaf, FAQPage su homepage)
    - Interlinking bidirezionale su ogni leaf page service×zone
    - Zero chiamate Directus o AI al build time
  </what-built>
  <how-to-verify>
    ```bash
    # 1. Avvia preview locale
    cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base
    npx astro preview --port 4321
    ```

    Verificare nel browser (http://localhost:4321):
    a. Homepage carica → http://localhost:4321/
    b. Una service hub page → http://localhost:4321/ristrutturazioni-interni/
    c. Una leaf page → http://localhost:4321/ristrutturazioni-interni/formia/
    d. Sulla leaf page: verificare presenza dei link di interlinking bidirezionale (service hub, zone hub, sorelle)
    e. Una zone hub → http://localhost:4321/zone/formia/
    f. Blog index → http://localhost:4321/blog/
    g. robots.txt → http://localhost:4321/robots.txt (deve contenere "Sitemap:")
    h. sitemap-index.xml → http://localhost:4321/sitemap-index.xml (deve essere XML valido)

    In DevTools (F12) su qualsiasi pagina:
    i. Aprire "Sources" o "Elements" → cercare `<script type="application/ld+json">`
    j. Verificare che il JSON contenga `"@graph"` e non `"name": null` (null-omission)
    k. Verificare che in nessun log di build appaia "directus" o "api/generate"
  </how-to-verify>
  <resume-signal>
    Digita "approvato" se tutto funziona correttamente.
    Oppure descrivi i problemi riscontrati (es. "404 su /zone/formia/", "JSON-LD mancante").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| robots.txt.ts → Astro.site | Il valore di site viene da astro.config.mjs — nessun input utente |
| dist/ → internet | L'output di build è pubblicamente accessibile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Information Disclosure | sitemap.xml espone struttura URL completa | accept | Intenzionalmente pubblico per SEO — la sitemap è il vettore di indicizzazione Google. Nessun URL privato esposto (il sito è completamente pubblico). |
| T-02-13 | Information Disclosure | robots.txt espone URL sitemap | accept | Standard SEO — tutti i siti lo fanno. Nessun secret incluso. |
| T-02-14 | Tampering | scripts/mock-factory.cjs usato solo in sviluppo | mitigate | Il file mock viene usato solo per smoke test locale. Il factoryApi viene ripristinato a produzione dopo la build. Il file mock NON deve essere deployato su Cloudflare Pages — verificare che .gitignore o wrangler ignore escluda scripts/ se necessario. |
</threat_model>

<verification>
1. `ls rankame/templates/astro-base/src/pages/robots.txt.ts` → esiste
2. `grep "sitemap-index.xml" rankame/templates/astro-base/src/pages/robots.txt.ts` → 1 match
3. `ls rankame/templates/astro-base/dist/sitemap-index.xml rankame/templates/astro-base/dist/robots.txt` → entrambi esistono
4. `grep -c "loc>" rankame/templates/astro-base/dist/sitemap-0.xml` → ≥ 40
5. `grep "example.com" rankame/templates/astro-base/dist/sitemap-0.xml` → 0 match
6. `grep "Sitemap: https://ristrutturazioniformia.it" rankame/templates/astro-base/dist/robots.txt` → 1 match
7. `grep "factory-core.soliwkr.workers.dev" rankame/templates/astro-base/src/data/site.config.json` → 1 match (factoryApi ripristinato)
8. `npx vitest run` → ancora tutti PASS
</verification>

<success_criteria>
**Successo = tutti e 4 i success criteria del ROADMAP §Phase 2 soddisfatti:**

1. SITE-01: Build produce homepage + 3 service hub + 9 zone hub + 27 leaf service×zone + blog index → confermato da count URL sitemap ≥ 40
2. SITE-02: Ogni pagina ha JSON-LD con @graph (LocalBusiness senza null, BreadcrumbList, Service su leaf, FAQPage su homepage) → confermato da checkpoint visivo DevTools
3. SITE-03: sitemap-index.xml e robots.txt presenti in dist/ con dominio corretto → confermato da `ls dist/`
4. SITE-04: Nessuna chiamata a Directus o /api/generate nei log di build → confermato da assenza di URL Directus nei log

**Checkpoint visivo approvato dall'utente.**
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-astro-template-completion/02-05-SUMMARY.md`
</output>
