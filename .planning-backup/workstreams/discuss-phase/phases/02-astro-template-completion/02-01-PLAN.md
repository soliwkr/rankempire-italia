---
phase: 02-astro-template-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - factory-core/src/db/schema.ts
  - factory-core/src/api/sites.ts
  - factory-core/src/index.ts
autonomous: true
requirements:
  - SITE-04
must_haves:
  truths:
    - "L'endpoint GET /api/sites/:projectId/pages risponde 200 con array di pagine quando il progetto esiste in D1"
    - "L'endpoint risponde 404 con messaggio chiaro quando projectId non ha pagine"
    - "La tabella D1 `pages` esiste con le colonne: id, project_id, slug, type, title, body, faq, meta, created_at"
    - "L'endpoint è pubblico — nessun Bearer token richiesto al fetch al build time"
  artifacts:
    - path: "factory-core/src/db/schema.ts"
      provides: "Tabella pages Drizzle ORM"
      contains: "sqliteTable('pages'"
    - path: "factory-core/src/api/sites.ts"
      provides: "Endpoint GET /api/sites/:projectId/pages"
      exports: ["default api"]
    - path: "factory-core/src/index.ts"
      provides: "Mount endpoint pubblico /api/sites"
      contains: "app.route('/api/sites', sitesApi)"
  key_links:
    - from: "factory-core/src/index.ts"
      to: "factory-core/src/api/sites.ts"
      via: "app.route('/api/sites', sitesApi)"
      pattern: "app\\.route.*api/sites"
    - from: "factory-core/src/api/sites.ts"
      to: "factory-core/src/db/schema.ts"
      via: "import { pages } from '../db/schema'"
      pattern: "from.*db/schema"
---

<objective>
Aggiungere la tabella D1 `pages` allo schema Drizzle e creare l'endpoint pubblico `GET /api/sites/:projectId/pages` in factory-core — il contratto API che il template Astro userà al build time per recuperare tutto il contenuto di un sito.

Purpose: Astro non può fare la build statica senza questo endpoint. È il fondamento su cui il resto della Fase 2 è costruito.
Output: Endpoint factory-core funzionante + schema D1 aggiornato + schema push applicato.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-astro-template-completion/02-CONTEXT.md
@.planning/phases/02-astro-template-completion/02-RESEARCH.md
@.planning/phases/02-astro-template-completion/02-PATTERNS.md

<interfaces>
<!-- Contratti esistenti da rispettare. -->

Da factory-core/src/db/schema.ts (pattern tabella con FK):
```typescript
import { sqliteTable, text, integer, real } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';

export const leads = sqliteTable('leads', {
  id: text('id').primaryKey(),
  projectId: text('project_id').notNull().references(() => projects.id),
  status: text('status').default('new').notNull(),
  createdAt: text('created_at').default(sql`CURRENT_TIMESTAMP`).notNull(),
});
```

Da factory-core/src/api/projects.ts (pattern Hono router):
```typescript
import { Hono } from 'hono';
import { drizzle } from 'drizzle-orm/d1';
import { eq } from 'drizzle-orm';
import { projects } from '../db/schema';

type Bindings = { DB: D1Database; /* altri binding */ };
const api = new Hono<{ Bindings: Bindings }>();

api.get('/:id', async (c) => {
  const db = drizzle(c.env.DB);
  const id = c.req.param('id');
  const project = await db.select().from(projects).where(eq(projects.id, id)).get();
  if (!project) return c.json({ error: 'Project not found' }, 404);
  return c.json(project);
});

export default api;
```

Da factory-core/src/index.ts (pattern mount pubblico):
```typescript
// Endpoint pubblici — nessun auth richiesto
app.route('/api/leads', leadsApi);

// Endpoint protetti — Bearer token richiesto
const protectedApp = new Hono<{ Bindings: Bindings }>();
protectedApp.use('/*', async (c, next) => { return bearerAuth({ token: c.env.API_SECRET })(c, next); });
protectedApp.route('/api/projects', projectsApi);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggiungere tabella `pages` a schema.ts + creare sites.ts</name>

  <read_first>
    - factory-core/src/db/schema.ts — pattern import, pattern tabella esistente leads (righe 26-37), struttura attuale del file
    - factory-core/src/api/projects.ts — pattern Hono router, tipo Bindings, pattern .where(eq()), pattern 404
  </read_first>

  <files>
    factory-core/src/db/schema.ts,
    factory-core/src/api/sites.ts
  </files>

  <behavior>
    - Test 1: `pages` table definition ha tutti i campi richiesti da D-05: id, projectId, slug, type, title, body, faq, meta, createdAt
    - Test 2: `faq` e `meta` hanno default JSON validi ('[]' e '{}' rispettivamente)
    - Test 3: `GET /api/sites/:projectId/pages` restituisce `{ pages: PageContent[] }` quando le righe esistono
    - Test 4: `GET /api/sites/:projectId/pages` restituisce 404 `{ error: string }` quando non esistono righe per quel projectId
  </behavior>

  <action>
1. **Modifica factory-core/src/db/schema.ts** — aggiungere DOPO la definizione di `leads`:
   ```typescript
   export const pages = sqliteTable('pages', {
     id: text('id').primaryKey(),
     projectId: text('project_id').notNull().references(() => projects.id),
     slug: text('slug').notNull(),
     type: text('type').notNull(), // 'homepage'|'service'|'zone'|'service_zone'|'blog'
     title: text('title').notNull(),
     body: text('body').notNull(),
     faq: text('faq').notNull().default('[]'),   // JSON string
     meta: text('meta').notNull().default('{}'), // JSON string
     createdAt: text('created_at').default(sql`CURRENT_TIMESTAMP`).notNull(),
   });
   ```
   La FK `references(() => projects.id)` segue esattamente il pattern di `leads.projectId`.

2. **Crea factory-core/src/api/sites.ts** — nuovo file, pattern identico a projects.ts:
   ```typescript
   import { Hono } from 'hono';
   import { drizzle } from 'drizzle-orm/d1';
   import { eq } from 'drizzle-orm';
   import { pages } from '../db/schema';

   type Bindings = { DB: D1Database };
   const api = new Hono<{ Bindings: Bindings }>();

   api.get('/:projectId/pages', async (c) => {
     const db = drizzle(c.env.DB);
     const projectId = c.req.param('projectId');
     const allPages = await db.select().from(pages).where(eq(pages.projectId, projectId)).all();
     if (allPages.length === 0) {
       return c.json({ error: `Nessun contenuto trovato per project ${projectId}` }, 404);
     }
     return c.json({ pages: allPages });
   });

   export default api;
   ```
   NOTA: l'endpoint è PUBBLICO (non protetto). Il contenuto dei siti è pubblicamente leggibile e Cloudflare Pages build non gestisce secrets per fetch al build time.
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx wrangler deploy --dry-run 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `factory-core/src/db/schema.ts` contiene `export const pages = sqliteTable('pages'` con tutti i 9 campi (id, project_id, slug, type, title, body, faq, meta, created_at)
    - `factory-core/src/api/sites.ts` esiste ed esporta `default api` (Hono router)
    - Il router ha route `/:projectId/pages` con handler GET
    - `wrangler deploy --dry-run` esce con codice 0 (nessun errore TypeScript)
  </acceptance_criteria>

  <done>schema.ts ha la tabella pages, sites.ts ha il router Hono, deploy dry-run verde</done>
</task>

<task type="auto">
  <name>Task 2: Mount endpoint pubblico in index.ts + [BLOCKING] schema push D1</name>

  <read_first>
    - factory-core/src/index.ts — struttura attuale, riga mount `/api/leads` (endpoint pubblico), riga import di leadsApi
    - factory-core/drizzle.config.ts — configurazione drizzle-kit per capire il comando push
    - factory-core/wrangler.toml — binding DB e database_name per il push locale
  </read_first>

  <files>
    factory-core/src/index.ts
  </files>

  <action>
1. **Modifica factory-core/src/index.ts** — aggiungere import e mount PUBBLICO di sitesApi:
   - Aggiungere import in testa: `import sitesApi from './api/sites';`
   - Aggiungere mount subito DOPO `app.route('/api/leads', leadsApi)` (nella sezione endpoint pubblici):
     ```typescript
     app.route('/api/sites', sitesApi);
     ```
   NON aggiungere a `protectedApp` — è intenzionalmente pubblico (vedi D-06 e Open Question 2).

2. **[BLOCKING] Push schema D1** — eseguire in factory-core/:
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
   npx wrangler d1 execute factory-db --local --command "SELECT name FROM sqlite_master WHERE type='table' AND name='pages';"
   ```
   Se la tabella `pages` non esiste ancora nel DB locale, eseguire il push dello schema:
   ```bash
   npx drizzle-kit push
   ```
   Se drizzle-kit push richiede input interattivo (es. conferma), rispondere `y` o usare il flag `--force` se disponibile.

   In alternativa, applicare direttamente via wrangler D1:
   ```bash
   npx wrangler d1 execute factory-db --local --command "CREATE TABLE IF NOT EXISTS pages (id TEXT PRIMARY KEY, project_id TEXT NOT NULL REFERENCES projects(id), slug TEXT NOT NULL, type TEXT NOT NULL, title TEXT NOT NULL, body TEXT NOT NULL, faq TEXT NOT NULL DEFAULT '[]', meta TEXT NOT NULL DEFAULT '{}', created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);"
   ```
   Verificare con: `npx wrangler d1 execute factory-db --local --command "SELECT name FROM sqlite_master WHERE type='table' AND name='pages';"` — deve restituire una riga.
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx wrangler d1 execute factory-db --local --command "SELECT name FROM sqlite_master WHERE type='table' AND name='pages';" 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `factory-core/src/index.ts` contiene la riga `import sitesApi from './api/sites';`
    - `factory-core/src/index.ts` contiene `app.route('/api/sites', sitesApi)` nella sezione endpoint PUBBLICI (prima di `protectedApp`)
    - `wrangler d1 execute factory-db --local --command "SELECT name FROM sqlite_master WHERE type='table' AND name='pages';"` restituisce almeno una riga con `name = "pages"`
    - `wrangler deploy --dry-run` esce con codice 0
  </acceptance_criteria>

  <done>sitesApi montato come endpoint pubblico, tabella pages presente nel DB locale</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→GET /api/sites/:projectId/pages | Qualsiasi client può leggere il contenuto di un sito dato un projectId |
| GET response→Astro build | Il JSON restituito viene iniettato nelle pagine HTML — body HTML incluso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | GET /api/sites/:projectId/pages | accept | L'endpoint è intenzionalmente pubblico — il contenuto dei siti è pubblico per design (SEO). Il projectId non è segreto. |
| T-02-02 | Tampering | body HTML da D1 → set:html in Astro | mitigate | Il body è generato esclusivamente da Phase 3 (Gemini via factory-core) — non da input utente. Documentare esplicitamente nel codice: "body non deve contenere `<script>` tag — generato solo da Phase 3". La logica di sanitizzazione verrà aggiunta in Phase 3 prima della scrittura su D1. |
| T-02-03 | Spoofing | factoryApi URL in site.config.json | accept | La config è iniettata da Phase 4 (processo interno controllato). Nessun input utente raggiunge factoryApi. Rischio accettabile per v1. |
| T-02-04 | Enumeration | GET /api/sites/:projectId/pages | accept | Senza auth, un attaccante può iterare projectId. Per v1 con dati pubblici SEO è accettabile. Auth opzionale in Phase 9 se necessario. |
</threat_model>

<verification>
1. `cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx wrangler deploy --dry-run` → exit 0
2. `grep "pages" factory-core/src/db/schema.ts` → almeno 1 match con `sqliteTable('pages'`
3. `grep "sitesApi" factory-core/src/index.ts` → almeno 2 match (import + route)
4. `grep "app.route.*api/sites" factory-core/src/index.ts` → 1 match nella sezione pubblica
5. `npx wrangler d1 execute factory-db --local --command "SELECT name FROM sqlite_master WHERE type='table';"` → include `pages`
</verification>

<success_criteria>
- L'endpoint GET /api/sites/:projectId/pages è raggiungibile localmente (via wrangler dev o curl) e risponde 404 su projectId inesistente
- La tabella `pages` è presente nel DB locale con tutti i 9 campi richiesti da D-05
- deploy --dry-run verde (nessun errore TypeScript)
- index.ts monta sitesApi come endpoint PUBBLICO
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-astro-template-completion/02-01-SUMMARY.md`
</output>
