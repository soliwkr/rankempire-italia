---
phase: 04-deploy-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
autonomous: false
files_modified: []
requirements:
  - FACT-03
  - FACT-04
  - FACT-05

must_haves:
  truths:
    - "La migration D1 0004_add_deploy_columns.sql è stata applicata al database locale"
    - "Le 3 colonne (github_repo_url, pages_project_name, pages_url) esistono nella tabella projects di D1"
    - "La migration D1 0004_add_deploy_columns.sql è stata applicata al database remote (produzione)"
  artifacts:
    - path: "factory-core/migrations/0004_add_deploy_columns.sql"
      provides: "Migration SQL già applicata (locale + remote)"
      contains: "ALTER TABLE"
  key_links:
    - from: "factory-core/src/db/schema.ts"
      to: "D1 database (locale e remote)"
      via: "npx wrangler d1 migrations apply factory-db"
      pattern: "github_repo_url.*pages_project_name.*pages_url"
---

<objective>
Questo piano applica la migration D1 `0004_add_deploy_columns.sql` sia in locale che in produzione — operazione bloccante che deve completarsi prima che qualsiasi test del deploy endpoint possa funzionare.

Purpose: [BLOCKING] — senza le 3 colonne nel database, il deploy endpoint fallisce con errore schema al primo UPDATE status. Nessun piano successivo può passare verification senza questa migration applicata.
Output: Database D1 locale e remote con colonne github_repo_url, pages_project_name, pages_url nella tabella projects.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-deploy-pipeline/04-CONTEXT.md

<interfaces>
<!-- Stato attuale migration directory (da leggere al momento dell'esecuzione) -->
<!-- factory-core/migrations/ -->
<!--   0000_glossy_madame_masque.sql -->
<!--   0001_typical_the_renegades.sql -->
<!--   0002_medical_mauler.sql -->
<!--   0003_petite_jack_murdock.sql  (aggiunto email a leads) -->
<!--   0004_add_deploy_columns.sql   (creato da piano 04-01) -->

<!-- Comandi wrangler D1 -->
<!-- Locale:  npx wrangler d1 migrations apply factory-db --local -->
<!-- Remote:  npx wrangler d1 migrations apply factory-db -->
<!-- Verifica locale: npx wrangler d1 execute factory-db --local --command "PRAGMA table_info(projects)" -->
<!-- Verifica remote: npx wrangler d1 execute factory-db --command "PRAGMA table_info(projects)" -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: [BLOCKING] Applicare migration D1 locale e remote</name>
  <read_first>
    - factory-core/migrations/0004_add_deploy_columns.sql (confermare che il file esiste e contiene le 3 ALTER TABLE prima di applicare)
    - factory-core/wrangler.toml (per verificare che database_name = "factory-db" sia il target corretto)
  </read_first>
  <what-built>
    Piano 04-01 ha creato il file `factory-core/migrations/0004_add_deploy_columns.sql` con:
    ```sql
    ALTER TABLE `projects` ADD `github_repo_url` text;--> statement-breakpoint
    ALTER TABLE `projects` ADD `pages_project_name` text;--> statement-breakpoint
    ALTER TABLE `projects` ADD `pages_url` text;
    ```
    Questa migration deve essere applicata al database D1 sia in locale (per testing) che in produzione (per il Worker live).
  </what-built>
  <action>
    Eseguire i seguenti comandi dalla directory `factory-core/`:

    **Step 1 — Applica migration locale:**
    ```bash
    cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
    npx wrangler d1 migrations apply factory-db --local
    ```
    Se richiede conferma interattiva, rispondere "y" / premere Invio.

    **Step 2 — Verifica colonne in locale:**
    ```bash
    npx wrangler d1 execute factory-db --local \
      --command "PRAGMA table_info(projects)"
    ```
    Deve mostrare le 3 nuove colonne: `github_repo_url`, `pages_project_name`, `pages_url`.

    **Step 3 — Applica migration remote (produzione):**
    ```bash
    npx wrangler d1 migrations apply factory-db
    ```
    Se richiede conferma interattiva, rispondere "y".

    **Step 4 — Verifica colonne in remote:**
    ```bash
    npx wrangler d1 execute factory-db \
      --command "PRAGMA table_info(projects)"
    ```
    Deve mostrare le stesse 3 colonne.
  </action>
  <how-to-verify>
    Dopo aver eseguito entrambi gli apply, confermare l'output di PRAGMA:

    Output atteso (estratto — non necessariamente nell'ordine esatto):
    ```
    github_repo_url  TEXT  0  NULL  0
    pages_project_name  TEXT  0  NULL  0
    pages_url  TEXT  0  NULL  0
    ```

    Tutte e 3 le colonne devono apparire sia nel risultato locale che nel risultato remote.
  </how-to-verify>
  <resume-signal>Scrivi "migration-ok" quando entrambe le migration (locale e remote) sono state applicate con successo e PRAGMA mostra le 3 colonne. Descrivi eventuali errori.</resume-signal>
  <acceptance_criteria>
    - `npx wrangler d1 execute factory-db --local --command "PRAGMA table_info(projects)"` include le righe per `github_repo_url`, `pages_project_name`, `pages_url` nell'output
    - `npx wrangler d1 execute factory-db --command "PRAGMA table_info(projects)"` include le stesse 3 colonne (remote)
    - Nessuno dei due comandi ritorna errore "no such column" o "already exists" — wrangler skipppa automaticamente le migration già applicate
    - Il comando apply non mostra "FAILED" nell'output
  </acceptance_criteria>
  <done>Tabella projects in D1 (locale e remote) ha le 3 nuove colonne nullable per deploy state tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrangler CLI → D1 remote | Il comando usa le credenziali Cloudflare dell'operatore (CLOUDFLARE_API_TOKEN env o login wrangler) per modificare il database di produzione |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | D1 remote schema (apply irreversibile) | accept | ALTER TABLE ADD COLUMN è operazione sicura e additiva — non modifica dati esistenti, non rimuove colonne. Rollback non necessario: colonne nullable, nessun dato existing rotto |
| T-04-04-02 | Denial of Service | Migration applicata due volte | accept | Wrangler D1 traccia le migration applicate — se 0004 è già applicata, viene skippata automaticamente senza errore |
| T-04-04-03 | Elevation of Privilege | Credenziali wrangler con accesso D1 | accept | Operazione eseguita dall'operatore con le sue credenziali Cloudflare. Scope: Database D1 del progetto. Nessuna espansione di privilege oltre l'accesso già necessario per deploy |
</threat_model>

<verification>
```bash
# Verifica colonne post-migration
cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core

# Locale
npx wrangler d1 execute factory-db --local \
  --command "SELECT name FROM pragma_table_info('projects') WHERE name IN ('github_repo_url','pages_project_name','pages_url')"

# Remote
npx wrangler d1 execute factory-db \
  --command "SELECT name FROM pragma_table_info('projects') WHERE name IN ('github_repo_url','pages_project_name','pages_url')"
```
Entrambi devono restituire 3 righe.
</verification>

<success_criteria>
- Migration 0004_add_deploy_columns.sql applicata correttamente a D1 locale
- Migration 0004_add_deploy_columns.sql applicata correttamente a D1 remote (produzione)
- PRAGMA table_info(projects) mostra github_repo_url, pages_project_name, pages_url su entrambi i database
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-deploy-pipeline/04-04-SUMMARY.md`
</output>
