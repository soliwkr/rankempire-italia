---
phase: 04-deploy-pipeline
plan: 06
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-05"
files_modified: []
autonomous: false
requirements:
  - FACT-01
  - FACT-03
  - FACT-04
  - FACT-05

must_haves:
  truths:
    - "POST /api/projects/:id/deploy risponde 401 senza Bearer token"
    - "POST /api/projects/:id/deploy crea un repo GitHub rr-{slug} sotto StudioPuraLuce (o il github owner del template)"
    - "Il repo creato contiene src/data/site.config.json con niche, city, factoryApi e projectId corretti"
    - "POST /api/projects/:id/deploy crea un progetto Cloudflare Pages rr-{slug} collegato al repo"
    - "POST /api/projects/:id/deploy aggiorna status D1 a 'deploying'"
    - "Una seconda chiamata allo stesso endpoint (post-deploy) risponde 409 se status è 'live', oppure riprende idempotentemente"
  artifacts:
    - path: "factory-core/src/api/projects.ts"
      provides: "Deploy endpoint operativo e verificato end-to-end"
      contains: "deploying"
  key_links:
    - from: "curl POST /api/projects/:id/deploy"
      to: "GitHub API repo + CF Pages project"
      via: "GitHubService + CloudflarePagesService"
      pattern: "status.*deploying"
---

<objective>
Smoke test E2E del deploy pipeline: verifica che l'intero flusso funzioni end-to-end su un progetto reale in D1 — dalla chiamata HTTP al repo GitHub creato + Pages project linked + D1 aggiornato a 'deploying'.

Purpose: Nessuna delle verifiche precedenti ha testato il flusso integrato con GitHub API e CF Pages API reali. Questo piano chiude il loop.
Output: Conferma visiva e API del flusso completo; progetto test in stato 'deploying' su D1.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-deploy-pipeline/04-CONTEXT.md
@.planning/phases/04-deploy-pipeline/04-05-SUMMARY.md

<interfaces>
<!-- Worker locale: npx wrangler dev --port 8787 (dalla directory factory-core) -->
<!-- API_SECRET per dev: API_SECRET=dev-secret-local (da .dev.vars) -->
<!-- CF_API_TOKEN: variabile reale richiesta — va in .dev.vars come CF_API_TOKEN=... -->
<!-- GITHUB_TOKEN: già in .dev.vars -->

<!-- Comandi di verifica post-deploy -->
<!-- 1. GET repo GitHub creato -->
<!-- curl -s -H "Authorization: Bearer $GITHUB_TOKEN" https://api.github.com/repos/{owner}/rr-{slug} | jq '{name, html_url}' -->
<!-- 2. GET site.config.json nel repo -->
<!-- curl -s -H "Authorization: Bearer $GITHUB_TOKEN" https://api.github.com/repos/{owner}/rr-{slug}/contents/src/data/site.config.json | jq '.content' | base64 -d -->
<!-- 3. GET CF Pages project -->
<!-- curl -s -H "Authorization: Bearer $CF_API_TOKEN" https://api.cloudflare.com/client/v4/accounts/$CF_ACCOUNT_ID/pages/projects/rr-{slug} | jq '.result.name' -->
<!-- 4. GET D1 status -->
<!-- npx wrangler d1 execute factory-db --command "SELECT id, slug, status, github_repo_url, pages_url FROM projects WHERE slug='rr-{slug}'" -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Setup .dev.vars con CF_API_TOKEN per test locale</name>
  <read_first>
    - factory-core/.dev.vars (per vedere i valori attuali e confermare che CF_API_TOKEN non è ancora presente)
  </read_first>
  <what-built>
    Il deploy endpoint richiede CF_API_TOKEN per chiamare la Cloudflare Pages API. Questa variabile è un secret che non va in wrangler.toml ma deve essere in .dev.vars per l'ambiente locale di test.
  </what-built>
  <action>
    Aggiungere `CF_API_TOKEN` a `factory-core/.dev.vars`:

    1. Aprire factory-core/.dev.vars
    2. Aggiungere la riga:
       ```
       CF_API_TOKEN=cfat_Szy9MAB0qeZ04HwRAvXw4jjEBI5JbBzODkdaH14V119a0b47
       ```
       (Usa il token CLOUDFLARE_API_TOKEN già presente nel file — è lo stesso token con Pages:Edit scope)

    3. Aggiungere anche:
       ```
       GITHUB_TEMPLATE_OWNER=StudioPuraLuce
       GITHUB_TEMPLATE_REPO=astro-rank-rent
       FACTORY_API_URL=https://factory-core.soliwkr.workers.dev
       ```

    Nota: .dev.vars non viene mai committato (è in .gitignore di default per i progetti Wrangler).
  </action>
  <how-to-verify>
    ```bash
    grep 'CF_API_TOKEN\|GITHUB_TEMPLATE_OWNER\|FACTORY_API_URL' factory-core/.dev.vars
    ```
    Deve mostrare le 4 righe aggiunte.
  </how-to-verify>
  <resume-signal>Scrivi "devvars-ok" quando .dev.vars è aggiornato.</resume-signal>
  <acceptance_criteria>
    - `grep 'CF_API_TOKEN' factory-core/.dev.vars | grep -v '^#'` restituisce una riga con il token reale
    - `grep 'GITHUB_TEMPLATE_OWNER=StudioPuraLuce' factory-core/.dev.vars` restituisce la riga
    - `grep 'FACTORY_API_URL' factory-core/.dev.vars` restituisce la riga con l'URL produzione
  </acceptance_criteria>
  <done>.dev.vars pronto per il test locale del deploy endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test E2E — crea progetto test e lancia deploy</name>
  <files></files>
  <read_first>
    - factory-core/.dev.vars (per confermare che API_SECRET, CF_API_TOKEN, GITHUB_TOKEN, GITHUB_TEMPLATE_OWNER sono presenti)
    - factory-core/src/api/projects.ts (per confermare la firma del POST / e POST /:id/deploy dopo la riscrittura)
  </read_first>
  <action>
    Eseguire la sequenza di comandi da `factory-core/` per testare l'intero flusso E2E. Prima avviare il Worker locale in background, poi testare:

    **Step 1 — Avvia Worker locale:**
    ```bash
    cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
    npx wrangler dev --port 8787 &
    WRANGLER_PID=$!
    sleep 3  # attendi startup
    ```

    **Step 2 — Crea un progetto test in D1:**
    ```bash
    curl -s -X POST http://localhost:8787/api/projects \
      -H "Authorization: Bearer dev-secret-local" \
      -H "Content-Type: application/json" \
      -d '{"slug":"test-deploy-smoke","name":"Test Deploy Smoke","niche":"idraulico","location":"Formia"}' \
      | jq '{id, success}'
    ```
    Salvare l'`id` restituito come `PROJECT_ID`.

    **Step 3 — Chiama il deploy endpoint:**
    ```bash
    curl -s -X POST http://localhost:8787/api/projects/${PROJECT_ID}/deploy \
      -H "Authorization: Bearer dev-secret-local" \
      -H "Content-Type: application/json" \
      -d '{"services":["riparazione-perdite","installazione-caldaie"],"zones":["centro","nord"],"avatar":"in-pain"}' \
      | jq .
    ```
    Output atteso: `{ "success": true, "repoUrl": "https://github.com/...", "pagesUrl": "https://rr-test-deploy-smoke.pages.dev", "status": "deploying" }`

    **Step 4 — Verifica auth gate (senza token deve dare 401):**
    ```bash
    curl -s -X POST http://localhost:8787/api/projects/${PROJECT_ID}/deploy \
      -H "Content-Type: application/json" | jq '.error // .message'
    ```
    Output atteso: stringa contenente "Unauthorized" o simile.

    **Step 5 — Ferma il Worker locale:**
    ```bash
    kill $WRANGLER_PID 2>/dev/null || true
    ```
  </action>
  <verify>
    <automated>
      cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx wrangler d1 execute factory-db --local --command "SELECT slug, status, github_repo_url, pages_url FROM projects WHERE slug='test-deploy-smoke'" 2>&1
    </automated>
  </verify>
  <acceptance_criteria>
    - La risposta al deploy (Step 3) contiene `"success": true` e `"status": "deploying"` e `"repoUrl"` non vuoto
    - La risposta senza auth (Step 4) è 401 (non 200)
    - `npx wrangler d1 execute factory-db --local --command "SELECT status FROM projects WHERE slug='test-deploy-smoke'"` restituisce `deploying` (non `pending`)
    - `github_repo_url` nel record D1 contiene `https://github.com/` e il nome repo `rr-test-deploy-smoke`
    - `pages_url` nel record D1 contiene `.pages.dev`
  </acceptance_criteria>
  <done>Deploy endpoint conferma la pipeline completa: auth gate 401, repo GitHub creato, D1 aggiornato a 'deploying', pagesUrl non vuoto.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Checkpoint visivo — verifica GitHub repo + CF Pages dashboard</name>
  <read_first>
    - Nessuno — questo è un checkpoint di verifica visiva sui servizi esterni
  </read_first>
  <what-built>
    Il deploy E2E ha creato:
    1. Un repo GitHub `rr-test-deploy-smoke` (o `rr-{slug}`) sotto `StudioPuraLuce`
    2. Un progetto Cloudflare Pages `rr-test-deploy-smoke` collegato al repo
    Il file `src/data/site.config.json` nel repo deve contenere niche, city, services, zones, avatar corretti.
  </what-built>
  <how-to-verify>
    **Verifica GitHub:**
    1. Aprire https://github.com/StudioPuraLuce/rr-test-deploy-smoke
    2. Navigare in `src/data/site.config.json`
    3. Confermare che il file contenga:
       - `"projectId"`: UUID valido
       - `"niche"`: "idraulico"
       - `"city"`: "Formia"
       - `"services"`: ["riparazione-perdite", "installazione-caldaie"]
       - `"zones"`: ["centro", "nord"]
       - `"avatar"`: "in-pain"
       - `"factoryApi"`: "https://factory-core.soliwkr.workers.dev"
       - `"domain"`: null

    **Verifica Cloudflare Pages:**
    1. Aprire https://dash.cloudflare.com → Account → Pages
    2. Cercare il progetto `rr-test-deploy-smoke`
    3. Confermare che sia collegato al repo `StudioPuraLuce/rr-test-deploy-smoke`
    4. Verificare che sia stato triggerato un build automatico

    **Verifica idempotency:**
    ```bash
    # Aggiornare manualmente lo status a 'live' per testare il 409
    npx wrangler d1 execute factory-db --local \
      --command "UPDATE projects SET status='live', pages_url='https://rr-test-deploy-smoke.pages.dev' WHERE slug='test-deploy-smoke'"

    # Richiamare il deploy — deve restituire 409
    curl -s -X POST http://localhost:8787/api/projects/${PROJECT_ID}/deploy \
      -H "Authorization: Bearer dev-secret-local" \
      -H "Content-Type: application/json" | jq '{error, pages_url}'
    ```
    Output atteso: 409 con `"error": "Project already deployed"` e `pages_url`.
  </how-to-verify>
  <resume-signal>Scrivi "phase4-ok" se tutti e 3 i controlli (GitHub repo + CF Pages + idempotency 409) passano. Descrivi eventuali problemi.</resume-signal>
  <acceptance_criteria>
    - GitHub repo `StudioPuraLuce/rr-test-deploy-smoke` esiste e contiene `src/data/site.config.json` con i campi corretti
    - CF Pages dashboard mostra il progetto `rr-test-deploy-smoke` collegato al repo GitHub
    - Il re-deploy con status='live' restituisce HTTP 409 con `"error": "Project already deployed"`
  </acceptance_criteria>
  <done>Pipeline completa verificata visivamente: repo GitHub + site.config.json + CF Pages project + idempotency 409.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .dev.vars → Worker locale | CF_API_TOKEN reale in file locale — non deve essere committato |
| smoke test → GitHub API reale | Crea repo reale sotto StudioPuraLuce — non è un sandbox |
| smoke test → CF Pages API reale | Crea Pages project reale sull'account Cloudflare — ha costi minimi ma è reale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Information Disclosure | .dev.vars committato per errore | mitigate | Verificare prima del test che `git status` non mostri `.dev.vars` come staged. `.dev.vars` deve essere in .gitignore — se non lo è, aggiungerlo prima di procedere |
| T-04-06-02 | Denial of Service | Smoke test crea risorse reali (repo GitHub + CF Pages project) | accept | Il repo test `rr-test-deploy-smoke` è un repo privato sotto StudioPuraLuce. Il Pages project usa il piano gratuito CF. Pulizia manuale post-test: eliminare repo GitHub e CF Pages project se non necessari |
| T-04-06-03 | Information Disclosure | PROJECT_ID in shell history | accept | Il PROJECT_ID è uno UUID non-sensibile. La shell history locale non è un vettore di attacco per questo caso d'uso |
</threat_model>

<verification>
Verifiche finali di fase:

```bash
# Riepilogo successo criteria Phase 4
echo "=== Phase 4 Deploy Pipeline Verification ==="

# 1. Schema OK
grep -c 'githubRepoUrl\|pagesProjectName\|pagesUrl' factory-core/src/db/schema.ts && echo "OK: schema 3 colonne"

# 2. Migration OK
[ -f factory-core/migrations/0004_add_deploy_columns.sql ] && echo "OK: migration file exists"

# 3. Services OK
grep -c 'waitForRepo' factory-core/src/services/github.ts && echo "OK: github retry loop"
[ -f factory-core/src/services/cloudflare-pages.ts ] && echo "OK: CF Pages service exists"

# 4. Endpoint OK
grep -c 'pages_linked\|repo_created\|deploying' factory-core/src/api/projects.ts && echo "OK: state machine"

# 5. wrangler.toml OK
grep -c 'GITHUB_TEMPLATE_OWNER\|FACTORY_API_URL' factory-core/wrangler.toml && echo "OK: env vars"

# 6. TypeScript compila
cd factory-core && npx wrangler deploy --dry-run 2>&1 | grep -i 'error' | head -3
```
</verification>

<success_criteria>
- Auth gate: POST /api/projects/:id/deploy senza token → 401
- GitHub repo `rr-{slug}` creato sotto StudioPuraLuce con `src/data/site.config.json` corretto (tutti i 14 campi SiteConfig)
- CF Pages project `rr-{slug}` creato e linked al repo GitHub, build triggerato
- D1 status aggiornato a 'deploying' con github_repo_url e pages_url popolati
- Idempotency: re-deploy su progetto 'live' → 409 con pages_url e github_repo_url
- Nessun sleep hack (`setTimeout.*2000`) in nessun file della pipeline
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/04-deploy-pipeline/04-06-SUMMARY.md`
</output>
