---
phase: 02-astro-template-completion
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - rankame/templates/astro-base/astro.config.mjs
  - rankame/templates/astro-base/package.json
  - rankame/templates/astro-base/src/data/site.config.json
  - rankame/templates/astro-base/src/lib/fetchPages.ts
  - rankame/templates/astro-base/src/lib/buildSchemaGraph.ts
  - rankame/templates/astro-base/src/lib/buildPaths.ts
  - rankame/templates/astro-base/src/types/site.ts
  - rankame/templates/astro-base/src/components/SchemaGraph.astro
  - rankame/templates/astro-base/src/components/Breadcrumb.astro
  - rankame/templates/astro-base/src/components/InternalLinks.astro
  - rankame/templates/astro-base/src/layouts/Layout.astro
autonomous: true
requirements:
  - SITE-02
  - SITE-04
must_haves:
  truths:
    - "npx vitest run passa con tutti i test verdi (GREEN) — i test scritti in Piano 02 diventano verdi"
    - "astro.config.mjs legge src/data/site.config.json (non config.json) e imposta site dinamicamente"
    - "SchemaGraph.astro omette i campi null in ghost mode — mai placeholder inventati"
    - "fetchPages.ts lancia errore chiaro con hint 'Phase 3' su slug mancante"
    - "buildPaths.ts enumera services × zones come params stringa"
  artifacts:
    - path: "rankame/templates/astro-base/src/lib/fetchPages.ts"
      provides: "Fetch centralizzato contenuto D1 con cache e fail-fast"
      exports: ["fetchAllPages", "requirePage", "PageContent"]
    - path: "rankame/templates/astro-base/src/lib/buildSchemaGraph.ts"
      provides: "Funzione pura per costruzione @graph JSON-LD"
      exports: ["buildSchemaGraph"]
    - path: "rankame/templates/astro-base/src/lib/buildPaths.ts"
      provides: "Funzione pura per enumerazione route getStaticPaths"
      exports: ["buildServiceZonePaths"]
    - path: "rankame/templates/astro-base/src/components/SchemaGraph.astro"
      provides: "Componente Astro che emette <script type='application/ld+json'>"
    - path: "rankame/templates/astro-base/src/layouts/Layout.astro"
      provides: "Layout base esteso con canonical, sitemap link, slot head"
  key_links:
    - from: "rankame/templates/astro-base/src/components/SchemaGraph.astro"
      to: "rankame/templates/astro-base/src/lib/buildSchemaGraph.ts"
      via: "import { buildSchemaGraph } from '../lib/buildSchemaGraph'"
      pattern: "buildSchemaGraph"
    - from: "rankame/templates/astro-base/astro.config.mjs"
      to: "rankame/templates/astro-base/src/data/site.config.json"
      via: "readFileSync('./src/data/site.config.json')"
      pattern: "site\\.config\\.json"
---

<objective>
Creare la layer di astrazione fondamentale del template Astro: configurazione corretta, utility TypeScript (`fetchPages`, `buildSchemaGraph`, `buildPaths`), tipi condivisi, e componenti riutilizzabili (`SchemaGraph`, `Breadcrumb`, `InternalLinks`) — rendendo verdi i test del Piano 02.

Purpose: Senza queste utility e componenti, le pagine Astro (Piano 04) non possono essere costruite. Questo piano crea i contratti che tutto il routing consumerà.
Output: Tutti i test del Piano 02 diventano verdi. Layout e componenti pronti per le pagine del Piano 04.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-astro-template-completion/02-CONTEXT.md
@.planning/phases/02-astro-template-completion/02-RESEARCH.md
@.planning/phases/02-astro-template-completion/02-PATTERNS.md
@.planning/phases/02-astro-template-completion/02-02-SUMMARY.md

<interfaces>
<!-- Contratti che questo piano deve implementare per soddisfare i test del Piano 02. -->

Test attesi in tests/fetch-pages.test.ts (già scritti, devono diventare verdi):
```typescript
// requirePage() deve lanciare Error con lo slug e "Phase 3"
export function requirePage(pages: PageContent[], slug: string): PageContent
// fetchAllPages() deve chiamare fetch con ${factoryApi}/api/sites/${projectId}/pages
export async function fetchAllPages(): Promise<PageContent[]>
```

Test attesi in tests/schema-graph.test.ts:
```typescript
// buildSchemaGraph(config, props) → { "@context": "https://schema.org", "@graph": [...] }
// ghost mode: nodo LocalBusiness senza name, telephone, address, geo
// service_zone: @graph contiene { "@type": "Service", areaServed: { "@type": "City", name: zoneSlug } }
// homepage con faqs: @graph contiene { "@type": "FAQPage" }
// tutte le pagine: @graph contiene { "@type": "BreadcrumbList" }
export function buildSchemaGraph(config: SiteConfig, props: SchemaProps): SchemaOutput
```

Test attesi in tests/routing.test.ts:
```typescript
// buildServiceZonePaths(services, zones, pages) → Array<{ params: { service: string; zone: string }, props: { content: PageContent } }>
// lancia Error su slug mancante
export function buildServiceZonePaths(services: string[], zones: string[], pages: PageContent[]): StaticPath[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: astro.config.mjs + package.json + src/data/site.config.json + types + lib utilities</name>

  <read_first>
    - rankame/templates/astro-base/astro.config.mjs — file ATTUALE da SOSTITUIRE (leggere per capire cosa rimuovere: config.json, site hardcoded, Vite define)
    - rankame/templates/astro-base/package.json — verificare che @astrojs/sitemap NON sia già presente nelle dipendenze
    - rankame/templates/astro-base/src/data/ — verificare se esiste già config.json da eliminare o aggiornare
    - .planning/phases/02-astro-template-completion/02-RESEARCH.md §Pattern 1, §Pattern 2 — codice esatto da usare
  </read_first>

  <files>
    rankame/templates/astro-base/astro.config.mjs,
    rankame/templates/astro-base/package.json,
    rankame/templates/astro-base/src/data/site.config.json,
    rankame/templates/astro-base/src/types/site.ts,
    rankame/templates/astro-base/src/lib/fetchPages.ts,
    rankame/templates/astro-base/src/lib/buildPaths.ts
  </files>

  <behavior>
    - Test A: astro.config.mjs legge './src/data/site.config.json' (non 'config.json')
    - Test B: astro.config.mjs imposta site: `https://${siteConfig.domain}` (non hardcoded 'example.com')
    - Test C: astro.config.mjs NON usa Vite define per iniettare SITE_CONFIG
    - Test D: fetchAllPages() chiama fetch con URL contenente '/api/sites/test-project-001/pages'
    - Test E: requirePage() lancia Error con slug e 'Phase 3' su slug mancante
    - Test F: buildServiceZonePaths(services, zones, pages) restituisce services.length × zones.length paths
    - Test G: ogni path da buildServiceZonePaths ha params.service e params.zone come string
  </behavior>

  <action>
1. **Installa @astrojs/sitemap** (nuova dipendenza):
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base && npm install @astrojs/sitemap
   ```

2. **Sostituisci rankame/templates/astro-base/astro.config.mjs** con:
   ```javascript
   import { defineConfig } from 'astro/config';
   import tailwindcss from '@tailwindcss/vite';
   import sitemap from '@astrojs/sitemap';
   import { readFileSync } from 'node:fs';
   import { resolve } from 'node:path';

   let siteConfig = { domain: 'example.com', niche: 'Default', city: 'City' };
   try {
     const configPath = resolve('./src/data/site.config.json');
     siteConfig = JSON.parse(readFileSync(configPath, 'utf-8'));
   } catch (e) {
     console.warn('[astro.config] site.config.json non trovato — uso default. Phase 4 iniettera questo file al deploy.');
   }

   export default defineConfig({
     site: `https://${siteConfig.domain}`,
     trailingSlash: 'always',
     integrations: [sitemap()],
     vite: {
       plugins: [tailwindcss()],
       // RIMOSSO: nessun define — le pagine importano site.config.json direttamente
     },
   });
   ```
   CRITICO: rimuovere `define: { 'import.meta.env.SITE_CONFIG': ... }` — era il vecchio anti-pattern.

3. **Crea rankame/templates/astro-base/src/data/site.config.json** (esempio per sviluppo locale — Phase 4 sovrascrive questo file al deploy reale):
   ```json
   {
     "projectId": "dev-ristrutturazioni-formia",
     "niche": "ristrutturazioni",
     "city": "formia",
     "services": ["ristrutturazioni-interni", "rifacimento-bagno", "ristrutturazioni-esterni"],
     "zones": ["formia", "gaeta", "minturno", "fondi", "terracina", "sperlonga", "itri", "cassino", "frosinone"],
     "avatar": "in-pain",
     "factoryApi": "https://factory-core.soliwkr.workers.dev",
     "slug": "ristrutturazioni-formia",
     "domain": "ristrutturazioniformia.it",
     "gmbPlaceId": null,
     "ga4MeasurementId": null,
     "businessName": null,
     "phone": null,
     "address": null,
     "geo": null
   }
   ```

4. **Crea rankame/templates/astro-base/src/types/site.ts**:
   ```typescript
   export interface SiteConfig {
     projectId: string;
     niche: string;
     city: string;
     services: string[];
     zones: string[];
     avatar: string;
     factoryApi: string;
     slug: string;
     domain: string;
     gmbPlaceId: string | null;
     ga4MeasurementId: string | null;
     businessName: string | null;
     phone: string | null;
     address: Record<string, string> | null;
     geo: { latitude: number; longitude: number } | null;
   }

   export interface FaqItem {
     question: string;
     answer: string;
   }

   export interface PageContent {
     id: string;
     projectId: string;
     slug: string;
     type: 'homepage' | 'service' | 'zone' | 'service_zone' | 'blog';
     title: string;
     body: string;
     faq: FaqItem[];
     meta: { description: string; canonical: string };
   }

   export interface BreadcrumbItem {
     name: string;
     url: string;
   }
   ```

5. **Crea rankame/templates/astro-base/src/lib/fetchPages.ts**:
   ```typescript
   import siteConfig from '../data/site.config.json';

   export interface PageContent {
     id: string;
     projectId: string;
     slug: string;
     type: 'homepage' | 'service' | 'zone' | 'service_zone' | 'blog';
     title: string;
     body: string;
     faq: Array<{ question: string; answer: string }>;
     meta: { description: string; canonical: string };
   }

   let _cache: PageContent[] | null = null;

   export async function fetchAllPages(): Promise<PageContent[]> {
     if (_cache) return _cache;
     const url = `${siteConfig.factoryApi}/api/sites/${siteConfig.projectId}/pages`;
     const res = await fetch(url);
     if (!res.ok) {
       throw new Error(
         `[build] factory-core ${url} returned ${res.status} — aborting build`
       );
     }
     const data = await res.json() as { pages: PageContent[] };
     _cache = data.pages;
     return _cache;
   }

   export function requirePage(pages: PageContent[], slug: string): PageContent {
     const page = pages.find(p => p.slug === slug);
     if (!page) {
       throw new Error(
         `[build] Contenuto D1 mancante per slug "${slug}" — eseguire Phase 3 prima della build`
       );
     }
     return page;
   }
   ```

6. **Crea rankame/templates/astro-base/src/lib/buildPaths.ts**:
   ```typescript
   import type { PageContent } from './fetchPages';

   export interface StaticPath {
     params: { service: string; zone: string };
     props: { content: PageContent };
   }

   export function buildServiceZonePaths(
     services: string[],
     zones: string[],
     pages: PageContent[]
   ): StaticPath[] {
     const paths: StaticPath[] = [];
     for (const service of services) {
       for (const zone of zones) {
         const slug = `${service}/${zone}`;
         const content = pages.find(p => p.slug === slug);
         if (!content) {
           throw new Error(
             `[build] Contenuto D1 mancante per slug "${slug}" — eseguire Phase 3 prima della build`
           );
         }
         paths.push({
           params: { service: String(service), zone: String(zone) },
           props: { content }
         });
       }
     }
     return paths;
   }
   ```
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base && npx vitest run tests/fetch-pages.test.ts tests/routing.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep "site.config.json" rankame/templates/astro-base/astro.config.mjs` → 1 match
    - `grep "config.json" rankame/templates/astro-base/astro.config.mjs` → 0 match (il vecchio path è rimosso)
    - `grep "SITE_CONFIG" rankame/templates/astro-base/astro.config.mjs` → 0 match (Vite define rimosso)
    - `grep "@astrojs/sitemap" rankame/templates/astro-base/package.json` → 1 match
    - `cat rankame/templates/astro-base/src/data/site.config.json | python3 -c "import sys, json; d=json.load(sys.stdin); assert 'projectId' in d and 'services' in d and 'zones' in d"` → exit 0
    - I test fetch-pages e routing diventano verdi (`npx vitest run tests/fetch-pages.test.ts tests/routing.test.ts` → PASS)
  </acceptance_criteria>

  <done>astro.config.mjs corretto, lib utilities create, test fetchPages e routing verdi</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildSchemaGraph.ts + SchemaGraph.astro + Breadcrumb.astro + InternalLinks.astro + Layout.astro</name>

  <read_first>
    - rankame/templates/astro-base/src/layouts/Layout.astro — struttura ATTUALE da estendere (aggiungere canonical, sitemap link, slot head)
    - rankame/templates/astro-base/src/components/SEO.astro — vedere pattern set:html esistente (riga 16) — da NON duplicare in SchemaGraph
    - client-mgc-reparation/components/SchemaManager.tsx — reference @graph implementation (righe 98-228) — tradurre in Astro statico
    - .planning/phases/02-astro-template-completion/02-PATTERNS.md §SchemaGraph.astro, §Breadcrumb.astro, §InternalLinks.astro
  </read_first>

  <files>
    rankame/templates/astro-base/src/lib/buildSchemaGraph.ts,
    rankame/templates/astro-base/src/components/SchemaGraph.astro,
    rankame/templates/astro-base/src/components/Breadcrumb.astro,
    rankame/templates/astro-base/src/components/InternalLinks.astro,
    rankame/templates/astro-base/src/layouts/Layout.astro
  </files>

  <behavior>
    - Test A: buildSchemaGraph con ghost config → nodo LocalBusiness senza name/telephone/address/geo
    - Test B: buildSchemaGraph con pageType service_zone → @graph ha nodo Service con areaServed.name = zoneSlug
    - Test C: buildSchemaGraph con faqs non vuoto e pageType homepage → @graph ha nodo FAQPage
    - Test D: buildSchemaGraph sempre → @graph ha nodo BreadcrumbList
    - Test E: buildSchemaGraph ghost → @graph ha nodo LocalBusiness (o sottotipo) con url e areaServed
  </behavior>

  <action>
1. **Crea rankame/templates/astro-base/src/lib/buildSchemaGraph.ts** — funzione pura testabile:
   ```typescript
   import type { SiteConfig, FaqItem, BreadcrumbItem } from '../types/site';

   export interface SchemaProps {
     pageType: 'homepage' | 'service' | 'zone' | 'service_zone' | 'blog' | 'blog_post';
     serviceSlug?: string;
     zoneSlug?: string;
     faqs?: FaqItem[];
     breadcrumbs: BreadcrumbItem[];
   }

   export interface SchemaOutput {
     '@context': string;
     '@graph': Record<string, unknown>[];
   }

   const nicheTypeMap: Record<string, string> = {
     ristrutturazioni: 'HomeAndConstructionBusiness',
     idraulico: 'Plumber',
     elettricista: 'Electrician',
     pulizie: 'HousePainter',
     giardinaggio: 'LandscapeService',
   };

   export function buildSchemaGraph(config: SiteConfig, props: SchemaProps): SchemaOutput {
     const { pageType, serviceSlug, zoneSlug, faqs = [], breadcrumbs } = props;
     const siteUrl = `https://${config.domain}`;
     const graph: Record<string, unknown>[] = [];

     // 1. LocalBusiness — omette campi null (D-11)
     const localBusinessType = nicheTypeMap[config.niche] ?? 'LocalBusiness';
     const localBusiness: Record<string, unknown> = {
       '@type': localBusinessType,
       '@id': `${siteUrl}/#organization`,
       url: siteUrl,
       areaServed: config.zones.map(zone => ({ '@type': 'City', name: zone })),
     };
     if (config.businessName) localBusiness.name = config.businessName;
     if (config.phone) localBusiness.telephone = config.phone;
     if (config.address) {
       localBusiness.address = { '@type': 'PostalAddress', ...config.address, addressCountry: 'IT' };
     }
     if (config.geo) {
       localBusiness.geo = { '@type': 'GeoCoordinates', ...config.geo };
     }
     graph.push(localBusiness);

     // 2. Service node (D-13) — su service e service_zone
     if (serviceSlug && (pageType === 'service' || pageType === 'service_zone')) {
       graph.push({
         '@type': 'Service',
         serviceType: serviceSlug.replace(/-/g, ' '),
         provider: { '@id': `${siteUrl}/#organization` },
         areaServed: zoneSlug
           ? { '@type': 'City', name: zoneSlug }
           : config.zones.map(z => ({ '@type': 'City', name: z })),
       });
     }

     // 3. FAQPage (D-14) — su homepage e service con faqs non vuoti
     if (faqs.length > 0 && (pageType === 'homepage' || pageType === 'service')) {
       graph.push({
         '@type': 'FAQPage',
         mainEntity: faqs.map(f => ({
           '@type': 'Question',
           name: f.question,
           acceptedAnswer: { '@type': 'Answer', text: f.answer },
         })),
       });
     }

     // 4. BreadcrumbList (D-15) — tutte le pagine
     graph.push({
       '@type': 'BreadcrumbList',
       itemListElement: breadcrumbs.map((crumb, idx) => ({
         '@type': 'ListItem',
         position: idx + 1,
         name: crumb.name,
         item: crumb.url,
       })),
     });

     return { '@context': 'https://schema.org', '@graph': graph };
   }
   ```

2. **Crea rankame/templates/astro-base/src/components/SchemaGraph.astro**:
   ```astro
   ---
   import { buildSchemaGraph } from '../lib/buildSchemaGraph';
   import siteConfig from '../data/site.config.json';
   import type { FaqItem, BreadcrumbItem } from '../types/site';

   interface Props {
     pageType: 'homepage' | 'service' | 'zone' | 'service_zone' | 'blog' | 'blog_post';
     serviceSlug?: string;
     zoneSlug?: string;
     faqs?: FaqItem[];
     breadcrumbs: BreadcrumbItem[];
   }

   const { pageType, serviceSlug, zoneSlug, faqs = [], breadcrumbs } = Astro.props;

   const schema = buildSchemaGraph(siteConfig as any, { pageType, serviceSlug, zoneSlug, faqs, breadcrumbs });
   ---

   <script type="application/ld+json" set:html={JSON.stringify(schema)} />
   ```
   CRITICO: `set:html` è obbligatorio — bypassa la sanitizzazione Astro per JSON-LD. Non usare `{JSON.stringify(...)}`.

3. **Crea rankame/templates/astro-base/src/components/Breadcrumb.astro**:
   ```astro
   ---
   interface Props {
     items: Array<{ name: string; url: string }>;
   }
   const { items } = Astro.props;
   ---
   <nav aria-label="breadcrumb">
     <ol class="flex flex-wrap gap-1 text-sm text-gray-500">
       {items.map((item, i) => (
         <li class="flex items-center">
           {i < items.length - 1
             ? <a href={item.url} class="hover:text-gray-900 underline">{item.name}</a>
             : <span aria-current="page" class="text-gray-900">{item.name}</span>
           }
           {i < items.length - 1 && <span class="mx-1" aria-hidden="true">/</span>}
         </li>
       ))}
     </ol>
   </nav>

4. **Crea rankame/templates/astro-base/src/components/InternalLinks.astro**:
   ```astro
   ---
   import siteConfig from '../data/site.config.json';

   interface Props {
     currentService?: string;
     currentZone?: string;
     pageType: 'service' | 'zone' | 'service_zone';
   }
   const { currentService, currentZone, pageType } = Astro.props;

   // Link alle pagine sorelle — calcolati al build time (D-09)
   const siblingServiceLinks = (currentZone && pageType === 'service_zone')
     ? siteConfig.services
         .filter(s => s !== currentService)
         .map(s => ({ href: `/${s}/${currentZone}/`, label: s.replace(/-/g, ' ') }))
     : [];

   const siblingZoneLinks = (currentService && pageType === 'service_zone')
     ? siteConfig.zones
         .filter(z => z !== currentZone)
         .map(z => ({ href: `/${currentService}/${z}/`, label: z }))
     : [];

   const allZoneLinks = (pageType === 'service' && currentService)
     ? siteConfig.zones.map(z => ({ href: `/${currentService}/${z}/`, label: z }))
     : [];

   const allServiceLinks = (pageType === 'zone' && currentZone)
     ? siteConfig.services.map(s => ({ href: `/${s}/${currentZone}/`, label: s.replace(/-/g, ' ') }))
     : [];
   ---

   <aside class="mt-8 border-t pt-6">
     {siblingServiceLinks.length > 0 && (
       <div class="mb-4">
         <h3 class="text-sm font-semibold text-gray-700 mb-2">Altri servizi a {currentZone}</h3>
         <ul class="flex flex-wrap gap-2">
           {siblingServiceLinks.map(l => (
             <li><a href={l.href} class="text-sm text-blue-600 hover:underline">{l.label}</a></li>
           ))}
         </ul>
       </div>
     )}
     {siblingZoneLinks.length > 0 && (
       <div class="mb-4">
         <h3 class="text-sm font-semibold text-gray-700 mb-2">{currentService?.replace(/-/g, ' ')} in altre zone</h3>
         <ul class="flex flex-wrap gap-2">
           {siblingZoneLinks.map(l => (
             <li><a href={l.href} class="text-sm text-blue-600 hover:underline">{l.label}</a></li>
           ))}
         </ul>
       </div>
     )}
     {allZoneLinks.length > 0 && (
       <div class="mb-4">
         <h3 class="text-sm font-semibold text-gray-700 mb-2">Zone servite</h3>
         <ul class="flex flex-wrap gap-2">
           {allZoneLinks.map(l => (
             <li><a href={l.href} class="text-sm text-blue-600 hover:underline">{l.label}</a></li>
           ))}
         </ul>
       </div>
     )}
     {allServiceLinks.length > 0 && (
       <div class="mb-4">
         <h3 class="text-sm font-semibold text-gray-700 mb-2">Servizi disponibili in questa zona</h3>
         <ul class="flex flex-wrap gap-2">
           {allServiceLinks.map(l => (
             <li><a href={l.href} class="text-sm text-blue-600 hover:underline">{l.label}</a></li>
           ))}
         </ul>
       </div>
     )}
   </aside>
   ```

5. **Estendi rankame/templates/astro-base/src/layouts/Layout.astro** — aggiungere canonical, sitemap link, slot head alle props esistenti:
   ```astro
   ---
   import '../styles/global.css';

   interface Props {
     title: string;
     description?: string;
     canonical?: string;
   }
   const { title, description = 'Sito Rank and Rent professionale', canonical } = Astro.props;
   ---
   <!doctype html>
   <html lang="it">
     <head>
       <meta charset="UTF-8" />
       <meta name="viewport" content="width=device-width, initial-scale=1.0" />
       <meta name="description" content={description} />
       {canonical && <link rel="canonical" href={canonical} />}
       <link rel="sitemap" href="/sitemap-index.xml" />
       <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
       <meta name="generator" content={Astro.generator} />
       <title>{title}</title>
       <slot name="head" />
     </head>
     <body class="bg-white text-gray-900 min-h-screen">
       <slot />
     </body>
   </html>
   ```
   NOTA: `<slot name="head" />` permette alle pagine di iniettare `<SchemaGraph />` nel head.
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base && npx vitest run 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run` → tutti i test PASS (verde) — zero fallimenti
    - `grep "buildSchemaGraph" rankame/templates/astro-base/src/components/SchemaGraph.astro` → 1 match
    - `grep "set:html" rankame/templates/astro-base/src/components/SchemaGraph.astro` → 1 match (pattern JSON-LD corretto)
    - `grep "canonical" rankame/templates/astro-base/src/layouts/Layout.astro` → almeno 1 match
    - `grep "sitemap-index.xml" rankame/templates/astro-base/src/layouts/Layout.astro` → 1 match
    - `grep "slot name=\"head\"" rankame/templates/astro-base/src/layouts/Layout.astro` → 1 match
  </acceptance_criteria>

  <done>Tutti i test verdi, componenti e layout pronti per Piano 04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D1 content → set:html in Layout | Il campo body HTML viene renderizzato direttamente nell'HTML della pagina |
| site.config.json → astro.config.mjs | Il dominio letto dal config è usato come `site:` per URL absoluti nella sitemap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | body HTML da PageContent → Fragment set:html | mitigate | Documentare nel codice che il campo `body` è prodotto esclusivamente da Phase 3 (Gemini via factory-core) — nessun input utente. Commento esplicito in fetchPages.ts: "// ATTENZIONE: body è HTML raw — Phase 3 è responsabile della sanitizzazione prima della scrittura su D1. Non usare set:html su contenuto da input utente." |
| T-02-07 | Information Disclosure | src/data/site.config.json nel repo | accept | Contiene solo dati di sviluppo locale. Phase 4 sovrascrive questo file nel repo del sito deployato — non nel repo template. Nessun secret nel file. |
| T-02-08 | Spoofing | JSON import da site.config.json in ogni componente | accept | Il JSON è importato staticamente al build time — nessun input utente, nessuna eval(). Rischio accettabile. |
</threat_model>

<verification>
1. `cd rankame/templates/astro-base && npx vitest run` → exit 0, tutti i test PASS
2. `grep "site.config.json" rankame/templates/astro-base/astro.config.mjs` → 1 match
3. `ls rankame/templates/astro-base/src/lib/` → mostra fetchPages.ts, buildSchemaGraph.ts, buildPaths.ts
4. `ls rankame/templates/astro-base/src/components/` → mostra SchemaGraph.astro, Breadcrumb.astro, InternalLinks.astro
5. `grep "slot name=\"head\"" rankame/templates/astro-base/src/layouts/Layout.astro` → 1 match
</verification>

<success_criteria>
- npx vitest run → tutti PASS (inclusi fetch-pages, schema-graph, routing)
- astro.config.mjs legge site.config.json, imposta site dinamico, include sitemap integration, rimuove Vite define
- Tutti i componenti e utility creati nei path attesi
- Layout esteso con canonical, sitemap link, slot head
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-astro-template-completion/02-03-SUMMARY.md`
</output>
