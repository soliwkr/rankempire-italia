---
phase: 06-lead-capture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - factory-core/src/api/lead-capture.ts
  - factory-core/src/services/email.ts
  - factory-core/src/services/telegram.ts
  - factory-core/schema.sql
autonomous: true
requirements:
  - LEAD-01
  - LEAD-02
  - LEAD-03
  - LEAD-04
  - LEAD-05
  - LEAD-06
user_setup:
  - service: Telegram
    why: "Lead notification alerts"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather"
      - name: TELEGRAM_CHAT_ID
        source: "Telegram API"
  - service: Resend
    why: "Transactional emails"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard"

must_haves:
  truths:
    - "Bot submissions are dropped via honeypot"
    - "Verified leads trigger Telegram notifications"
    - "Lead status updates to verified after DOI"
  artifacts:
    - path: "factory-core/src/api/lead-capture.ts"
      provides: "Lead submission and verification endpoint"
    - path: "factory-core/schema.sql"
      provides: "Lead table with status and avatar tagging"
  key_links:
    - from: "Lead form"
      to: "factory-core/api/lead-capture"
      via: "POST request with honeypot"
    - from: "factory-core/api/lead-capture"
      to: "Telegram API"
      via: "Notify on verified lead"
---

<objective>
Implement lead capture features: GDPR honeypot, avatar tagging, DOI verification flow, and Telegram notifications.

Purpose: Automate the acquisition and qualification of leads in a compliant manner.
Output: Backend logic and D1 schema changes to support the full lead lifecycle.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.gemini/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update D1 Schema and Backend Logic</name>
  <files>factory-core/schema.sql, factory-core/src/api/lead-capture.ts</files>
  <action>
    1. Update D1 schema for `leads` table to include `avatar_tag` (for LEAD-06) and `doi_token`.
    2. Implement `POST /api/leads` with honeypot check (LEAD-01, LEAD-02).
    3. Implement `GET /api/leads/verify/:token` (LEAD-04).
  </action>
  <verify>
    <automated>sqlite3 factory-core/.wrangler/state/d1/miniflare-D1DatabaseObject/db.sqlite "PRAGMA table_info(leads)"</automated>
  </verify>
  <done>Schema updated and lead capture/verify endpoints implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Resend and Telegram</name>
  <files>factory-core/src/services/email.ts, factory-core/src/services/telegram.ts</files>
  <action>
    1. Create Resend email service for DOI (LEAD-03).
    2. Create Telegram notification service (LEAD-05).
    3. Wire services into lead verification process.
  </action>
  <verify>
    <automated>npm run test -- --filter=email</automated>
  </verify>
  <done>Email and Telegram notification services operational.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Site → Factory API | Publicly accessible endpoint, high risk of spam/bots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-LEAD-01 | Tampering | Lead submission | Mitigate | Validate all inputs with Zod, enforce honeypot. |
| T-LEAD-02 | Info Disclosure | DOI token | Mitigate | Use secure, unique tokens, do not leak tokens in logs. |
| T-LEAD-03 | Denial of Service | Email/Telegram API | Mitigate | Implement rate limiting on lead endpoint. |
</threat_model>

<verification>
Automated tests verify schema and endpoint logic. End-to-end UAT required to verify Resend and Telegram flow.
</verification>

<success_criteria>
- Submission with honeypot field returns 200 but drops silently (no D1 write).
- Submission without honeypot creates D1 record (pending).
- Verification email sent.
- Verification link updates D1 record to verified.
- Verified lead triggers Telegram notification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-lead-capture/06-03-SUMMARY.md`
</output>
