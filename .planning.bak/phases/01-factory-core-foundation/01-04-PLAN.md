# PLAN 01-04: Integrity Restoration & Security Hardening

## Goal
Restore integrity to Phase 1 by ensuring authentication middleware and DOI workflows are fully operational and validated via E2E testing.

## Task Breakdown
1. **Config Verification:**
   - Add missing `API_SECRET` and `VERIFICATION_BASE_URL` to `factory-core/.dev.vars` (mocked).
   - Verify `wrangler.toml` configuration.
2. **Middleware Hardening:**
   - Add diagnostic logging to `factory-core/src/index.ts` to trace auth attempts.
   - Force 401 response on missing `API_SECRET`.
3. **DOI Diagnostic:**
   - Add validation check in `factory-core/src/api/leads.ts` to ensure `verificationBaseUrl` is not null/undefined before `EmailService` usage.
4. **Validation Test:**
   - Create `factory-core/src/tests/e2e/auth-integrity.test.ts` to confirm 401 on missing/wrong token.
   - Create `factory-core/src/tests/e2e/doi-integrity.test.ts` to confirm DOI URL formation.

## Verification
- Run `vitest` for the new E2E tests.
- Confirm 401 status for `/api/projects` without a Bearer token.
- Confirm successful email URL formation in logs/tests.
