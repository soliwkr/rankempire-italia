---
phase: 07-seo-tracking-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - factory-core/src/db/schema.ts
  - factory-core/src/services/google-analytics.ts
  - factory-core/src/services/search-console.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
user_setup:
  - service: Google Cloud Console
    why: "Access to GA4 Admin API and Search Console API"
    env_vars:
      - name: GOOGLE_SERVICE_ACCOUNT_CREDENTIALS
        source: "GCP Console -> IAM & Admin -> Service Accounts -> Create Key (JSON)"
    dashboard_config:
      - task: "Enable APIs"
        location: "GCP APIs & Services -> Library -> Search for Google Analytics Admin API and Google Search Console API"
must_haves:
  truths:
    - "New project deploy triggers GA4 property creation"
    - "New project deploy verifies domain in GSC"
    - "New project deploy submits sitemap to GSC"
  artifacts:
    - path: "factory-core/src/services/google-analytics.ts"
      provides: "GA4 Admin API wrapper"
      exports: ["createGA4Property"]
    - path: "factory-core/src/services/search-console.ts"
      provides: "GSC API wrapper"
      exports: ["verifyDomain", "submitSitemap"]
    - path: "factory-core/src/db/schema.ts"
      provides: "Updated schema with GA4/GSC tracking fields"
  key_links:
    - from: "factory-core/src/services/google-analytics.ts"
      to: "factory-core/src/services/search-console.ts"
      via: "Integration in deploy pipeline"
---

<objective>
Implementare servizi di base per GA4 e GSC e aggiornare lo schema database per memorizzare gli ID delle property.

Purpose: Fornire i mattoni fondamentali per l'automazione SEO.
Output: Servizi API pronti per essere integrati nel pipeline di deploy.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.gemini/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@factory-core/src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update D1 Schema</name>
  <files>factory-core/src/db/schema.ts</files>
  <action>Aggiungere i campi `ga4_property_id` e `gsc_verified` (boolean) alla tabella `projects`.</action>
  <verify>
    <automated>grep -q "ga4_property_id" factory-core/src/db/schema.ts && grep -q "gsc_verified" factory-core/src/db/schema.ts</automated>
  </verify>
  <done>Schema aggiornato con campi per tracciamento SEO.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GA4 and GSC services</name>
  <files>factory-core/src/services/google-analytics.ts, factory-core/src/services/search-console.ts</files>
  <action>Creare i wrapper utilizzando @googleapis/analyticsdata e @googleapis/searchconsole. Utilizzare google-auth-library per l'autenticazione tramite Service Account.</action>
  <verify>
    <automated>test -f factory-core/src/services/google-analytics.ts && test -f factory-core/src/services/search-console.ts</automated>
  </verify>
  <done>Servizi creati e pronti per l'uso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Account -> GCP APIs | Credenziali sensibili inviate per autenticarsi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SEO-01 | Elevation | Service Account | mitigate | Conservare in Cloudflare Secrets (non commit su repo) |
| T-SEO-02 | Information Disclosure | D1 Schema | accept | GA4 Property ID non è dato sensibile |
</threat_model>

<verification>
Verificare che i file siano creati e che le dipendenze siano importate correttamente.
</verification>

<success_criteria>
- Schema D1 aggiornato.
- Servizi GA4/GSC esistenti.
</success_criteria>

<output>
Dopo completamento, creare `.planning/phases/07-seo-tracking-automation/07-01-SUMMARY.md`
</output>
