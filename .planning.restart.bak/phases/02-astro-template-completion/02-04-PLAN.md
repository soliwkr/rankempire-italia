---
phase: 02-astro-template-completion
plan: 04
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - rankame/templates/astro-base/src/pages/index.astro
  - rankame/templates/astro-base/src/pages/[service]/index.astro
  - rankame/templates/astro-base/src/pages/[service]/[zone].astro
  - rankame/templates/astro-base/src/pages/zone/[zone].astro
  - rankame/templates/astro-base/src/pages/blog/index.astro
  - rankame/templates/astro-base/src/pages/blog/[slug].astro
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-04
must_haves:
  truths:
    - "Homepage fetcha contenuto da D1 (type: homepage) e lo renderizza senza chiamate AI"
    - "Le pagine service hub elencano tutte le zone disponibili con link"
    - "Le pagine service×zone (~72) includono interlinking bidirezionale verso hub padre e pagine sorelle"
    - "Le pagine zone hub elencano tutti i servizi disponibili con link"
    - "Blog index lista i post disponibili, blog/[slug] renderizza il singolo post"
    - "getStaticPaths() su ogni pagina dinamica lancia errore chiaro se il contenuto D1 è mancante"
    - "Ogni pagina include SchemaGraph nel <head> tramite slot name='head'"
  artifacts:
    - path: "rankame/templates/astro-base/src/pages/index.astro"
      provides: "Homepage — type: homepage"
    - path: "rankame/templates/astro-base/src/pages/[service]/index.astro"
      provides: "Service hub — type: service"
    - path: "rankame/templates/astro-base/src/pages/[service]/[zone].astro"
      provides: "Leaf service×zone — type: service_zone"
    - path: "rankame/templates/astro-base/src/pages/zone/[zone].astro"
      provides: "Zone hub — type: zone"
    - path: "rankame/templates/astro-base/src/pages/blog/index.astro"
      provides: "Blog index — list di type: blog"
    - path: "rankame/templates/astro-base/src/pages/blog/[slug].astro"
      provides: "Blog post singolo — type: blog"
  key_links:
    - from: "rankame/templates/astro-base/src/pages/[service]/[zone].astro"
      to: "rankame/templates/astro-base/src/lib/fetchPages.ts"
      via: "fetchAllPages() in getStaticPaths()"
      pattern: "fetchAllPages"
    - from: "rankame/templates/astro-base/src/pages/[service]/[zone].astro"
      to: "rankame/templates/astro-base/src/components/InternalLinks.astro"
      via: "<InternalLinks pageType='service_zone' />"
      pattern: "InternalLinks"
    - from: "tutte le pagine"
      to: "rankame/templates/astro-base/src/components/SchemaGraph.astro"
      via: "<SchemaGraph> nel slot head"
      pattern: "SchemaGraph"
---

<objective>
Creare le 6 pagine Astro che implementano il routing completo (D-08): homepage riscritta, 2 tipi di hub (service, zone), leaf service×zone, blog index e blog post — tutte con fetch da D1, SchemaGraph, Breadcrumb e interlinking bidirezionale.

Purpose: Queste pagine sono il prodotto finale della Fase 2. Senza di esse non esistono né route né HTML statico.
Output: 6 file `.astro` che coprono tutti i tipi di pagina richiesti da SITE-01.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-astro-template-completion/02-CONTEXT.md
@.planning/phases/02-astro-template-completion/02-PATTERNS.md
@.planning/phases/02-astro-template-completion/02-03-SUMMARY.md

<interfaces>
<!-- Contratti creati in Piano 03 — da usare direttamente. -->

Da src/lib/fetchPages.ts:
```typescript
export interface PageContent {
  id: string; projectId: string; slug: string;
  type: 'homepage' | 'service' | 'zone' | 'service_zone' | 'blog';
  title: string; body: string;
  faq: Array<{ question: string; answer: string }>;
  meta: { description: string; canonical: string };
}
export async function fetchAllPages(): Promise<PageContent[]>
export function requirePage(pages: PageContent[], slug: string): PageContent
```

Da src/lib/buildPaths.ts:
```typescript
export function buildServiceZonePaths(services: string[], zones: string[], pages: PageContent[]): StaticPath[]
// dove StaticPath = { params: { service: string; zone: string }, props: { content: PageContent } }
```

Da src/components/SchemaGraph.astro (Props):
```typescript
interface Props {
  pageType: 'homepage' | 'service' | 'zone' | 'service_zone' | 'blog' | 'blog_post';
  serviceSlug?: string;
  zoneSlug?: string;
  faqs?: FaqItem[];
  breadcrumbs: BreadcrumbItem[];
}
```

Da src/layouts/Layout.astro (Props):
```typescript
interface Props {
  title: string;
  description?: string;
  canonical?: string;
}
// Layout ha <slot name="head" /> per SchemaGraph
```

Da src/components/InternalLinks.astro (Props):
```typescript
interface Props {
  currentService?: string;
  currentZone?: string;
  pageType: 'service' | 'zone' | 'service_zone';
}
```

Da src/data/site.config.json:
```typescript
import siteConfig from '../../data/site.config.json';
// siteConfig.services: string[], siteConfig.zones: string[]
// siteConfig.domain: string, siteConfig.projectId: string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage + Service hub + Zone hub</name>

  <read_first>
    - rankame/templates/astro-base/src/pages/index.astro — file ATTUALE da RISCRIVERE completamente (leggere solo per capire cosa eliminare: import.meta.env.SITE_CONFIG, POST fetch, ecc.)
    - rankame/templates/astro-base/src/components/ContactForm.astro — verificare props richieste (projectId, projectName)
    - .planning/phases/02-astro-template-completion/02-PATTERNS.md §Pattern 3 getStaticPaths — pattern per hub
  </read_first>

  <files>
    rankame/templates/astro-base/src/pages/index.astro,
    rankame/templates/astro-base/src/pages/[service]/index.astro,
    rankame/templates/astro-base/src/pages/zone/[zone].astro
  </files>

  <action>
1. **RISCRIVI rankame/templates/astro-base/src/pages/index.astro** (eliminare TUTTO il contenuto attuale):
   ```astro
   ---
   import Layout from '../layouts/Layout.astro';
   import SchemaGraph from '../components/SchemaGraph.astro';
   import ContactForm from '../components/ContactForm.astro';
   import siteConfig from '../data/site.config.json';
   import { fetchAllPages, requirePage } from '../lib/fetchPages';

   const pages = await fetchAllPages();
   const content = requirePage(pages, 'homepage');
   const siteUrl = `https://${siteConfig.domain}`;

   const breadcrumbs = [{ name: 'Home', url: `${siteUrl}/` }];
   ---

   <Layout title={content.title} description={content.meta.description} canonical={`${siteUrl}/`}>
     <SchemaGraph
       slot="head"
       pageType="homepage"
       faqs={content.faq}
       breadcrumbs={breadcrumbs}
     />
     <main class="container mx-auto px-4 py-8 max-w-4xl">
       <Fragment set:html={content.body} />
       <ContactForm projectId={siteConfig.projectId} projectName={siteConfig.slug} />
     </main>
   </Layout>
   ```

2. **CREA rankame/templates/astro-base/src/pages/[service]/index.astro**:
   ```astro
   ---
   import Layout from '../../layouts/Layout.astro';
   import SchemaGraph from '../../components/SchemaGraph.astro';
   import InternalLinks from '../../components/InternalLinks.astro';
   import Breadcrumb from '../../components/Breadcrumb.astro';
   import ContactForm from '../../components/ContactForm.astro';
   import siteConfig from '../../data/site.config.json';
   import { fetchAllPages, requirePage } from '../../lib/fetchPages';
   import type { PageContent } from '../../lib/fetchPages';

   export async function getStaticPaths() {
     const pages = await fetchAllPages();
     return siteConfig.services.map(service => ({
       params: { service: String(service) },
       props: { content: requirePage(pages, service) }
     }));
   }

   const { content } = Astro.props as { content: PageContent };
   const { service } = Astro.params;
   const siteUrl = `https://${siteConfig.domain}`;

   const breadcrumbs = [
     { name: 'Home', url: `${siteUrl}/` },
     { name: service.replace(/-/g, ' '), url: `${siteUrl}/${service}/` }
   ];
   ---

   <Layout title={content.title} description={content.meta.description} canonical={`${siteUrl}/${service}/`}>
     <SchemaGraph
       slot="head"
       pageType="service"
       serviceSlug={service}
       faqs={content.faq}
       breadcrumbs={breadcrumbs}
     />
     <main class="container mx-auto px-4 py-8 max-w-4xl">
       <Breadcrumb items={breadcrumbs} />
       <Fragment set:html={content.body} />
       <InternalLinks currentService={service} pageType="service" />
       <ContactForm projectId={siteConfig.projectId} projectName={siteConfig.slug} />
     </main>
   </Layout>
   ```

3. **CREA rankame/templates/astro-base/src/pages/zone/[zone].astro**:
   ```astro
   ---
   import Layout from '../../layouts/Layout.astro';
   import SchemaGraph from '../../components/SchemaGraph.astro';
   import InternalLinks from '../../components/InternalLinks.astro';
   import Breadcrumb from '../../components/Breadcrumb.astro';
   import ContactForm from '../../components/ContactForm.astro';
   import siteConfig from '../../data/site.config.json';
   import { fetchAllPages, requirePage } from '../../lib/fetchPages';
   import type { PageContent } from '../../lib/fetchPages';

   export async function getStaticPaths() {
     const pages = await fetchAllPages();
     return siteConfig.zones.map(zone => ({
       params: { zone: String(zone) },
       props: { content: requirePage(pages, `zone/${zone}`) }
     }));
   }

   const { content } = Astro.props as { content: PageContent };
   const { zone } = Astro.params;
   const siteUrl = `https://${siteConfig.domain}`;

   const breadcrumbs = [
     { name: 'Home', url: `${siteUrl}/` },
     { name: 'Zone', url: `${siteUrl}/zone/` },
     { name: zone, url: `${siteUrl}/zone/${zone}/` }
   ];
   ---

   <Layout title={content.title} description={content.meta.description} canonical={`${siteUrl}/zone/${zone}/`}>
     <SchemaGraph
       slot="head"
       pageType="zone"
       zoneSlug={zone}
       breadcrumbs={breadcrumbs}
     />
     <main class="container mx-auto px-4 py-8 max-w-4xl">
       <Breadcrumb items={breadcrumbs} />
       <Fragment set:html={content.body} />
       <InternalLinks currentZone={zone} pageType="zone" />
       <ContactForm projectId={siteConfig.projectId} projectName={siteConfig.slug} />
     </main>
   </Layout>
   ```
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base && grep -l "fetchAllPages\|requirePage" src/pages/index.astro src/pages/\[service\]/index.astro src/pages/zone/\[zone\].astro 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `grep "import.meta.env.SITE_CONFIG" rankame/templates/astro-base/src/pages/index.astro` → 0 match (anti-pattern rimosso)
    - `grep "api/generate" rankame/templates/astro-base/src/pages/index.astro` → 0 match (nessuna chiamata AI al build)
    - `grep "fetchAllPages" rankame/templates/astro-base/src/pages/index.astro` → 1 match
    - `ls rankame/templates/astro-base/src/pages/[service]/index.astro` → file esiste
    - `ls rankame/templates/astro-base/src/pages/zone/[zone].astro` → file esiste
    - `grep "getStaticPaths" rankame/templates/astro-base/src/pages/\[service\]/index.astro` → 1 match
    - `grep "getStaticPaths" rankame/templates/astro-base/src/pages/zone/\[zone\].astro` → 1 match
    - `grep "SchemaGraph" rankame/templates/astro-base/src/pages/index.astro` → 1 match
  </acceptance_criteria>

  <done>Homepage riscritta senza anti-pattern, service hub e zone hub creati con getStaticPaths e SchemaGraph</done>
</task>

<task type="auto">
  <name>Task 2: Leaf service×zone + Blog index + Blog post</name>

  <read_first>
    - rankame/templates/astro-base/src/lib/buildPaths.ts — verifica firma: buildServiceZonePaths(services, zones, pages)
    - .planning/phases/02-astro-template-completion/02-PATTERNS.md §Pattern 3 — variante blog e service_zone
  </read_first>

  <files>
    rankame/templates/astro-base/src/pages/[service]/[zone].astro,
    rankame/templates/astro-base/src/pages/blog/index.astro,
    rankame/templates/astro-base/src/pages/blog/[slug].astro
  </files>

  <action>
1. **CREA rankame/templates/astro-base/src/pages/[service]/[zone].astro** — la pagina più importante (~72 pagine):
   ```astro
   ---
   import Layout from '../../layouts/Layout.astro';
   import SchemaGraph from '../../components/SchemaGraph.astro';
   import InternalLinks from '../../components/InternalLinks.astro';
   import Breadcrumb from '../../components/Breadcrumb.astro';
   import ContactForm from '../../components/ContactForm.astro';
   import siteConfig from '../../data/site.config.json';
   import { fetchAllPages } from '../../lib/fetchPages';
   import { buildServiceZonePaths } from '../../lib/buildPaths';
   import type { PageContent } from '../../lib/fetchPages';

   export async function getStaticPaths() {
     const pages = await fetchAllPages();
     return buildServiceZonePaths(siteConfig.services, siteConfig.zones, pages);
   }

   const { content } = Astro.props as { content: PageContent };
   const { service, zone } = Astro.params;
   const siteUrl = `https://${siteConfig.domain}`;

   const breadcrumbs = [
     { name: 'Home', url: `${siteUrl}/` },
     { name: service.replace(/-/g, ' '), url: `${siteUrl}/${service}/` },
     { name: zone, url: `${siteUrl}/${service}/${zone}/` }
   ];
   ---

   <Layout
     title={content.title}
     description={content.meta.description}
     canonical={`${siteUrl}/${service}/${zone}/`}
   >
     <SchemaGraph
       slot="head"
       pageType="service_zone"
       serviceSlug={service}
       zoneSlug={zone}
       faqs={content.faq}
       breadcrumbs={breadcrumbs}
     />
     <main class="container mx-auto px-4 py-8 max-w-4xl">
       <Breadcrumb items={breadcrumbs} />
       <Fragment set:html={content.body} />
       <InternalLinks currentService={service} currentZone={zone} pageType="service_zone" />
       <ContactForm projectId={siteConfig.projectId} projectName={siteConfig.slug} />
     </main>
   </Layout>
   ```

2. **CREA rankame/templates/astro-base/src/pages/blog/index.astro**:
   ```astro
   ---
   import Layout from '../../layouts/Layout.astro';
   import SchemaGraph from '../../components/SchemaGraph.astro';
   import Breadcrumb from '../../components/Breadcrumb.astro';
   import siteConfig from '../../data/site.config.json';
   import { fetchAllPages } from '../../lib/fetchPages';
   import type { PageContent } from '../../lib/fetchPages';

   const pages = await fetchAllPages();
   const blogPages: PageContent[] = pages.filter(p => p.type === 'blog');
   const siteUrl = `https://${siteConfig.domain}`;

   // Blog index non richiede slug D1 specifico — usa i post filtrati
   const breadcrumbs = [
     { name: 'Home', url: `${siteUrl}/` },
     { name: 'Blog', url: `${siteUrl}/blog/` }
   ];
   ---

   <Layout
     title={`Blog — ${siteConfig.niche} ${siteConfig.city}`}
     description={`Articoli su ${siteConfig.niche.replace(/-/g, ' ')} a ${siteConfig.city} e provincia.`}
     canonical={`${siteUrl}/blog/`}
   >
     <SchemaGraph
       slot="head"
       pageType="blog"
       breadcrumbs={breadcrumbs}
     />
     <main class="container mx-auto px-4 py-8 max-w-4xl">
       <Breadcrumb items={breadcrumbs} />
       <h1 class="text-3xl font-bold mb-6">Blog</h1>
       {blogPages.length === 0 && (
         <p class="text-gray-500">Articoli in arrivo.</p>
       )}
       <ul class="space-y-4">
         {blogPages.map(post => {
           const postSlug = post.slug.replace('blog/', '');
           return (
             <li>
               <a href={`/blog/${postSlug}/`} class="text-xl font-semibold text-blue-700 hover:underline">
                 {post.title}
               </a>
               <p class="text-gray-600 mt-1">{post.meta.description}</p>
             </li>
           );
         })}
       </ul>
     </main>
   </Layout>
   ```

3. **CREA rankame/templates/astro-base/src/pages/blog/[slug].astro**:
   ```astro
   ---
   import Layout from '../../layouts/Layout.astro';
   import SchemaGraph from '../../components/SchemaGraph.astro';
   import Breadcrumb from '../../components/Breadcrumb.astro';
   import siteConfig from '../../data/site.config.json';
   import { fetchAllPages } from '../../lib/fetchPages';
   import type { PageContent } from '../../lib/fetchPages';

   export async function getStaticPaths() {
     const pages = await fetchAllPages();
     const blogPages = pages.filter(p => p.type === 'blog');
     if (blogPages.length === 0) {
       // Nessun contenuto blog ancora (Phase 3 non eseguita) — build non genera pagine blog
       return [];
     }
     return blogPages.map(page => ({
       params: { slug: String(page.slug.replace('blog/', '')) },
       props: { content: page }
     }));
   }

   const { content } = Astro.props as { content: PageContent };
   const { slug } = Astro.params;
   const siteUrl = `https://${siteConfig.domain}`;

   const breadcrumbs = [
     { name: 'Home', url: `${siteUrl}/` },
     { name: 'Blog', url: `${siteUrl}/blog/` },
     { name: content.title, url: `${siteUrl}/blog/${slug}/` }
   ];
   ---

   <Layout
     title={content.title}
     description={content.meta.description}
     canonical={`${siteUrl}/blog/${slug}/`}
   >
     <SchemaGraph
       slot="head"
       pageType="blog_post"
       breadcrumbs={breadcrumbs}
     />
     <main class="container mx-auto px-4 py-8 max-w-4xl">
       <Breadcrumb items={breadcrumbs} />
       <article>
         <h1 class="text-3xl font-bold mb-6">{content.title}</h1>
         <Fragment set:html={content.body} />
       </article>
       <div class="mt-8">
         <a href="/blog/" class="text-blue-600 hover:underline">← Tutti gli articoli</a>
       </div>
     </main>
   </Layout>
   ```
   NOTA: `blog/[slug].astro` restituisce `[]` da getStaticPaths() se non ci sono post — la build non fallisce, genera semplicemente zero pagine blog. Questo è corretto perché Phase 3 non è ancora eseguita.
  </action>

  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/rankame/templates/astro-base && grep -l "getStaticPaths" src/pages/\[service\]/\[zone\].astro src/pages/blog/\[slug\].astro 2>&1 && ls src/pages/blog/index.astro</automated>
  </verify>

  <acceptance_criteria>
    - `ls rankame/templates/astro-base/src/pages/[service]/[zone].astro` → file esiste
    - `ls rankame/templates/astro-base/src/pages/blog/index.astro` → file esiste
    - `ls rankame/templates/astro-base/src/pages/blog/[slug].astro` → file esiste
    - `grep "buildServiceZonePaths" rankame/templates/astro-base/src/pages/\[service\]/\[zone\].astro` → 1 match
    - `grep "SchemaGraph" rankame/templates/astro-base/src/pages/\[service\]/\[zone\].astro` → 1 match con pageType="service_zone"
    - `grep "InternalLinks" rankame/templates/astro-base/src/pages/\[service\]/\[zone\].astro` → 1 match con pageType="service_zone"
    - Il blog/[slug].astro restituisce `[]` da getStaticPaths quando non ci sono pagine blog (no crash)
  </acceptance_criteria>

  <done>6 pagine Astro create — routing completo SITE-01, SchemaGraph su ogni pagina, interlinking su leaf pages</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D1 content.body → Fragment set:html | HTML grezzo da D1 renderizzato nella pagina |
| D1 content.faq → SchemaGraph JSON-LD | FAQ text da D1 iniettato nel JSON-LD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | content.body via Fragment set:html in tutte le pagine | mitigate | Commento in ogni pagina: "// ATTENZIONE: body è HTML generato da Phase 3 — mai iniettare contenuto da input utente". Phase 3 aggiunge sanitizzazione prima della scrittura su D1. Per v1 il flusso è completamente interno. |
| T-02-10 | Information Disclosure | BreadcrumbList in JSON-LD | accept | I breadcrumb sono URL pubblici del sito SEO — intenzionalmente pubblici. |
| T-02-11 | Denial of Service | getStaticPaths() throw su slug mancante | accept | La build fallisce esplicitamente — comportamento desiderato per prevenire siti con pagine vuote. Non è un vettore DoS (la build è un processo interno). |
</threat_model>

<verification>
1. `ls rankame/templates/astro-base/src/pages/` → mostra index.astro, [service]/, zone/, blog/
2. `ls rankame/templates/astro-base/src/pages/[service]/` → mostra index.astro e [zone].astro
3. `ls rankame/templates/astro-base/src/pages/blog/` → mostra index.astro e [slug].astro
4. `grep -r "import.meta.env.SITE_CONFIG" rankame/templates/astro-base/src/pages/` → 0 match
5. `grep -r "api/generate" rankame/templates/astro-base/src/pages/` → 0 match
6. `grep -r "fetchAllPages\|buildServiceZonePaths" rankame/templates/astro-base/src/pages/` → almeno 4 match
7. `npx vitest run` → tutti i test PASS ancora
</verification>

<success_criteria>
- Tutte le 6 pagine Astro esistono nei path corretti
- Nessuna pagina usa import.meta.env.SITE_CONFIG o chiama /api/generate
- Ogni pagina ha SchemaGraph nel slot head
- [service]/[zone].astro ha InternalLinks con interlinking bidirezionale
- blog/[slug].astro restituisce [] se non ci sono post (non crasha)
- npx vitest run ancora verde dopo le modifiche
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/02-astro-template-completion/02-04-SUMMARY.md`
</output>
