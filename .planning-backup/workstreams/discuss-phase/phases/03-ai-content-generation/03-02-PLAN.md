---
phase: 03-ai-content-generation
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - factory-core/src/services/prompts.ts
  - factory-core/src/api/seed.ts
  - factory-core/src/index.ts
autonomous: true
requirements:
  - FACT-02

must_haves:
  truths:
    - "PromptService ha 5 nuovi metodi per i 5 tipi di pagina"
    - "sanitizeHtml è esportata da seed.ts e rimuove script/iframe/handler"
    - "L'endpoint POST /api/generate/seed-project/:projectId?type=X esiste e risponde"
    - "Il tipo service_zones esegue un loop interno: N call Gemini (una per servizio), non una mega-call"
    - "Ogni call Gemini verifica finishReason === STOP prima di procedere"
    - "Il regex fallback estrae il JSON array anche se Gemini aggiunge testo preamble"
    - "Ogni batch di pagine è scritto su D1 immediatamente dopo la call Gemini (D-11)"
    - "I test stub del Piano 03-01 diventano GREEN"
  artifacts:
    - path: "factory-core/src/services/prompts.ts"
      provides: "5 nuovi metodi PromptService + struttura Gary Halbert per tutti i tipi"
      exports: ["generateHomepagePrompt", "generateServicesPrompt", "generateZonesPrompt", "generateServiceZonesPrompt", "generateBlogPrompt"]
    - path: "factory-core/src/api/seed.ts"
      provides: "Endpoint POST seed-project + sanitizeHtml esportata + parseGeminiJson + loop service_zones"
      exports: ["default (Hono router)", "sanitizeHtml"]
    - path: "factory-core/src/index.ts"
      provides: "Mount seed.ts su protectedApp (se route separata da generate.ts)"
  key_links:
    - from: "factory-core/src/api/seed.ts"
      to: "factory-core/src/services/prompts.ts"
      via: "import { PromptService } from '../services/prompts'"
      pattern: "new PromptService()"
    - from: "factory-core/src/api/seed.ts"
      to: "factory-core/src/services/ai.ts"
      via: "import { AiService } from '../services/ai'"
      pattern: "new AiService({ apiKey: c.env.GOOGLE_AI_API_KEY"
    - from: "factory-core/src/api/seed.ts"
      to: "D1 pages table"
      via: "db.insert(pages).values(rows).onConflictDoUpdate"
      pattern: "onConflictDoUpdate"
    - from: "factory-core/src/index.ts"
      to: "factory-core/src/api/seed.ts"
      via: "protectedApp.route('/api/generate', seedApi)"
      pattern: "seedApi"
---

<objective>
Wave 2 — Cuore della Phase 3: implementazione completa di PromptService (5 metodi) e dell'endpoint seed.

Questo piano implementa:
1. I 5 nuovi metodi di PromptService con prompt Gary Halbert per ogni tipo di pagina
2. La funzione `sanitizeHtml` (esportata — richiesta dai test del Piano 03-01)
3. Il nuovo file `factory-core/src/api/seed.ts` con l'endpoint `POST /api/generate/seed-project/:projectId?type=X`
4. Il loop interno per `service_zones` (N call Gemini per N servizi — CF-02 mitigation)
5. La validazione `finishReason` e il regex fallback JSON (CF-04 + Pitfall 4)
6. L'upsert D1 con `onConflictDoUpdate` su `(projectId, slug)` (D-04)
7. Il mount in `index.ts` se seed.ts è un file separato

Purpose: Rendere l'endpoint funzionale e tutti i test del Piano 03-01 verdi.
Output: prompts.ts esteso + seed.ts nuovo + index.ts aggiornato.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-content-generation/03-CONTEXT.md
@.planning/phases/03-ai-content-generation/03-RESEARCH.md
@.planning/phases/03-ai-content-generation/03-01-SUMMARY.md

<interfaces>
<!-- Contratti chiave che l'esecutore deve conoscere prima di iniziare. -->

Da factory-core/src/services/ai.ts (NON modificare):
```typescript
export class AiService {
  constructor(private config: AiConfig) {
    this.baseUrl = `https://gateway.ai.cloudflare.com/v1/${config.accountId}/${config.gatewayName}/google-ai-studio/v1beta/models/gemini-2.5-flash:generateContent`;
  }

  async generateContent(prompt: string, schema?: any): Promise<any>
  // schema truthy → responseMimeType: 'application/json' → ritorna JSON.parse(textContent)
  // ATTENZIONE: non espone finishReason — il caller deve fare la fetch raw oppure
  //             verificare la lunghezza/struttura dell'array risultante
  // SOLUZIONE: non usare generateContent() per seed; fare fetch raw con la stessa logica
  //            per poter accedere a result.candidates[0].finishReason
}
```

Da factory-core/src/db/schema.ts (dopo Piano 03-01):
```typescript
export const pages = sqliteTable('pages', {
  id: text('id').primaryKey(),
  projectId: text('project_id').notNull().references(() => projects.id),
  slug: text('slug').notNull(),
  type: text('type').notNull(),
  title: text('title').notNull(),
  body: text('body').notNull(),
  faq: text('faq').notNull().default('[]'),
  meta: text('meta').notNull().default('{}'),
  createdAt: text('created_at').default(sql`CURRENT_TIMESTAMP`).notNull(),
}, (table) => ({
  projectSlugUniq: uniqueIndex('pages_project_slug_uniq').on(table.projectId, table.slug),
}));

export const projects = sqliteTable('projects', {
  id: text('id').primaryKey(),
  niche: text('niche').notNull(),
  location: text('location').notNull(),
  configJson: text('config_json'),
  // ... altri campi
});
```

Da factory-core/src/services/prompts.ts (stato attuale — LEGGERE prima di estendere):
```typescript
export type AvatarType = 'in-pain' | 'skeptic' | 'bundler';

export interface ProjectContext {
  niche: string;
  city: string;
  neighborhoods?: string[];
}

export class PromptService {
  generatePrompt(context: ProjectContext, avatar: AvatarType): string { ... }
  // Contiene già: baseInstructions Gary Halbert, avatarSpecifics per 3 avatar
  // I 5 nuovi metodi vanno aggiunti nella stessa classe
}
```

Da factory-core/src/api/generate.ts (pattern da replicare esattamente):
```typescript
import { Hono } from 'hono';
import { AiService } from '../services/ai';
import { PromptService, type AvatarType } from '../services/prompts';

type Bindings = {
  DB: D1Database;
  GOOGLE_AI_API_KEY: string;
  CF_ACCOUNT_ID: string;
  CF_AI_GATEWAY_NAME: string;
  CF_AI_GATEWAY_TOKEN: string;
};

const api = new Hono<{ Bindings: Bindings }>();
// ... routes
export default api;
```

Da factory-core/src/api/leads.test.ts (contratto test che seed.test.ts deve soddisfare):
```typescript
// Il test chiama api.request(path, init, bindings)
// Il terzo argomento sono i Bindings mockati
// Per seed.test.ts: { DB: mockDb as any, GOOGLE_AI_API_KEY: 'test', ... }
```

Contratto PageContent da rankame/templates/astro-base/src/lib/fetchPages.ts:
```typescript
export interface PageContent {
  id: string;
  projectId: string;
  slug: string;
  type: 'homepage' | 'service' | 'zone' | 'service_zone' | 'blog';
  title: string;
  body: string;         // HTML sanitizzato
  faq: Array<{ question: string; answer: string }>;
  meta: { description: string; canonical: string };
}
// I dati scritti su D1 devono soddisfare ESATTAMENTE questo contratto
// faq e meta vanno serializzati come JSON string nel D1, deserializzati da GET /api/sites/:id/pages
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estendere PromptService con 5 metodi per tipo + sanitizeHtml utility</name>
  <files>factory-core/src/services/prompts.ts</files>
  <read_first>
    - factory-core/src/services/prompts.ts — leggere TUTTO: AvatarType, ProjectContext, baseInstructions, avatarSpecifics, generatePrompt esistente
    - .planning/phases/03-ai-content-generation/03-RESEARCH.md §"Italian SEO Content — Struttura Raccomandata per i Prompt" — anatomia pagina service_zone
    - .planning/phases/03-ai-content-generation/03-CONTEXT.md §D-06 — struttura JSON array attesa da Gemini
  </read_first>
  <behavior>
    - Test 1 (FACT-02-d): `service.generateHomepagePrompt({niche:'idraulico',city:'Formia'}, 'in-pain')` ritorna stringa che contiene 'IN-PAIN', 'Formia', 'RITORNA SOLO IL JSON ARRAY'
    - Test 2 (FACT-02-d): `service.generateServicesPrompt({niche:'idraulico',city:'Formia'}, ['riparazioni','installazioni'], 'skeptic')` ritorna stringa che contiene 'riparazioni' e 'installazioni'
    - Test 3 (FACT-02-d): `service.generateServiceZonesPrompt({niche:'idraulico',city:'Formia'}, 'riparazioni', ['Gaeta','Minturno'], 'bundler')` ritorna stringa che contiene 'riparazioni', 'Gaeta', 'Minturno'
    - Test 4 (FACT-02-d): `service.generateBlogPrompt({niche:'idraulico',city:'Formia'}, 'in-pain')` ritorna stringa che contiene 'idraulico', 'Formia', floor ≥3 menzionato nel prompt
    - Test 5 (FACT-02-e): `sanitizeHtml('<p>OK</p><script>alert(1)</script>')` → non contiene '<script>'
    - Test 6 (FACT-02-e): `sanitizeHtml('<p onclick="evil()">X</p>')` → non contiene 'onclick='
  </behavior>
  <action>
Aggiungere alla classe `PromptService` in `factory-core/src/services/prompts.ts` i 5 nuovi metodi.
Riusare `baseInstructions` e `avatarSpecifics` già presenti nella classe (estrarre come metodi privati o ripetere inline — Claude's discretion).

**Struttura comune per tutti i metodi:**
```
${baseInstructions Gary Halbert}
${avatarSpecifics[avatar]}

DATI PROGETTO:
Nicchia: ${niche}
Città: ${city}

[Istruzioni tipo-specifiche]

ESEMPIO DI OUTPUT (seguire ESATTAMENTE questa struttura JSON):
[
  {
    "slug": "slug-esempio",
    "type": "tipo_pagina",
    "title": "Titolo pagina",
    "body": "<p>Testo HTML semantico 400-600 parole con tag SOLO tra: <p> <h2> <h3> <ul> <li> <strong>. NO <script> NO <iframe>.</p>",
    "faq": [{"question": "Domanda 1?", "answer": "Risposta 1."}, {"question": "Domanda 2?", "answer": "Risposta 2."}, {"question": "Domanda 3?", "answer": "Risposta 3."}],
    "meta": {"description": "Descrizione SEO max 160 caratteri con keyword + città + CTA.", "canonical": "/slug-esempio/"}
  }
]

REGOLE CRITICHE:
- Ritorna SOLAMENTE il JSON array, nessun testo prima o dopo.
- Il campo body deve contenere 400-600 parole in HTML semantico.
- Ogni pagina deve citare ${city} almeno 2-3 volte in modo organico.
- Lingua: italiano naturale. NO termini in inglese. NO "aziendalese".
```

**Firma e comportamento atteso di ogni metodo:**

`generateHomepagePrompt(context: ProjectContext, avatar: AvatarType): string`
- Tipo pagina: `homepage`
- Slug: `homepage`
- Genera 1 pagina
- Prompt include: titolo H1 accattivante per la nicchia, sezione benefici, CTA avatar-specifica
- Esempio JSON nel prompt: 1 elemento con slug "homepage" e type "homepage"

`generateServicesPrompt(context: ProjectContext, services: string[], avatar: AvatarType): string`
- Tipo pagina: `service`
- Slug format: `{servizio-slugified}` (es. "riparazioni-perdite", "installazione-caldaie")
- Genera N pagine, una per ogni elemento di `services[]`
- Prompt elenca i servizi: `Genera UNA pagina per CIASCUNO dei seguenti servizi: ${services.join(', ')}`
- Esempio JSON nel prompt: 2 elementi con type "service"

`generateZonesPrompt(context: ProjectContext, zones: string[], avatar: AvatarType): string`
- Tipo pagina: `zone`
- Slug format: `{zona-slugified}` (es. "formia", "gaeta", "minturno")
- Genera N pagine, una per ogni elemento di `zones[]`
- Prompt elenca le zone: `Genera UNA pagina per CIASCUNA delle seguenti zone: ${zones.join(', ')}`
- Ogni pagina zone descrive la disponibilità del servizio in quella zona specifica
- Esempio JSON nel prompt: 2 elementi con type "zone"

`generateServiceZonesPrompt(context: ProjectContext, service: string, zones: string[], avatar: AvatarType): string`
- Tipo pagina: `service_zone`
- Slug format: `{servizio-slugified}-{zona-slugified}` (es. "riparazioni-perdite-gaeta")
- Genera M pagine (una per ogni zona) per UN SOLO servizio
- Prompt: `Per il servizio "${service}", genera UNA pagina per CIASCUNA zona: ${zones.join(', ')}`
- H1 obbligatorio: "${service} a ${zona}" — keyword principale
- Esempio JSON nel prompt: 2 elementi con type "service_zone"

`generateBlogPrompt(context: ProjectContext, avatar: AvatarType): string`
- Tipo pagina: `blog`
- Slug format: `blog-{argomento-slugified}`
- Genera K pagine (AI decide quante, MINIMO 3, MASSIMO 7)
- Prompt: `Genera tra 3 e 7 articoli blog informativi sulla nicchia "${niche}" a "${city}". Scegli argomenti pratici, guide, FAQ settoriali e confronti di servizi rilevanti per il pubblico locale.`
- Tono: informativo/educativo ma con voce avatar (meno urgenza, più contenuto)
- Esempio JSON nel prompt: 2 elementi con type "blog"

**Aggiungere anche `sanitizeHtml` come funzione esportata FUORI dalla classe** (così i test del Piano 03-01 possono importarla sia da `./prompts` sia verrà re-esportata da `./seed`):

```typescript
// Funzione esportata — richiesta da prompts.test.ts e re-esportata da seed.ts
export function sanitizeHtml(html: string): string {
  return html
    .replace(/<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, '')
    .replace(/<iframe\b[^>]*>[\s\S]*?<\/iframe>/gi, '')
    .replace(/\son\w+="[^"]*"/gi, '');
}
```

NOTA: Il test in `prompts.test.ts` importa `sanitizeHtml` da `'../api/seed'`. Aggiungere l'export in `seed.ts` nel Task 2 è sufficiente — NON spostare la funzione da `prompts.ts` a `seed.ts`. La funzione può stare in `prompts.ts` ed essere re-esportata da `seed.ts`.
  </action>
  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx vitest run src/services/prompts.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `prompts.ts` esporta `sanitizeHtml` come funzione standalone
    - `PromptService` ha i 5 nuovi metodi: `generateHomepagePrompt`, `generateServicesPrompt`, `generateZonesPrompt`, `generateServiceZonesPrompt`, `generateBlogPrompt`
    - `npx vitest run src/services/prompts.test.ts` passa tutti i test (GREEN)
    - Ogni metodo include nel prompt: baseInstructions Gary Halbert, avatarSpecifics[avatar], esempio JSON completo con tutti i campi del PageContent contract
    - Il prompt include esplicitamente "RITORNA SOLAMENTE il JSON array, nessun testo prima o dopo"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Creare seed.ts — endpoint + loop service_zones + finishReason + upsert D1</name>
  <files>
    factory-core/src/api/seed.ts
    factory-core/src/index.ts
  </files>
  <read_first>
    - factory-core/src/api/generate.ts — pattern Hono router, Bindings type, AiService instantiation, error handling (leggere TUTTO)
    - factory-core/src/services/ai.ts — firma generateContent(prompt, schema?) — IMPORTANTE: non espone finishReason, quindi seed.ts fa fetch raw diretta all'AI Gateway (vedi action)
    - factory-core/src/index.ts — pattern mount protectedApp.route() — leggere per sapere dove aggiungere il mount
    - factory-core/src/db/schema.ts — tabella pages (con uniqueIndex da Piano 03-01) e tabella projects
    - .planning/phases/03-ai-content-generation/03-RESEARCH.md §"Pattern Upsert D1" e §"Chunking per service_zones"
  </read_first>
  <behavior>
    - Test FACT-02-a: `api.request('/seed-project/proj-1', POST, no ?type)` → status 400
    - Test FACT-02-b: `api.request('/seed-project/proj-1?type=invalid', POST)` → status 400
    - Test FACT-02-c: DB mock ritorna null per select projects → status 404
    - Test FACT-02-f: `finishReason !== 'STOP'` → errore lanciato, endpoint ritorna 500
    - Test strutturale: `grep "for.*of.*services" seed.ts` trova il loop per service_zones
    - Test strutturale: `grep "finishReason" seed.ts` trova la validazione
    - Test strutturale: `grep "onConflictDoUpdate" seed.ts` trova l'upsert
    - Test strutturale: `grep "sanitizeHtml" seed.ts` trova la sanitizzazione applicata a body
  </behavior>
  <action>
Creare `factory-core/src/api/seed.ts` seguendo il pattern di `generate.ts` ma con logica estesa.

**Struttura completa del file:**

```typescript
import { Hono } from 'hono';
import { drizzle } from 'drizzle-orm/d1';
import { eq, sql } from 'drizzle-orm';
import { PromptService, sanitizeHtml, type AvatarType } from '../services/prompts';
import { projects, pages } from '../db/schema';
import { v4 as uuidv4 } from 'uuid'; // se uuid è disponibile, altrimenti crypto.randomUUID()

type Bindings = {
  DB: D1Database;
  GOOGLE_AI_API_KEY: string;
  CF_ACCOUNT_ID: string;
  CF_AI_GATEWAY_NAME: string;
  CF_AI_GATEWAY_TOKEN: string;
};

// Ri-esportare sanitizeHtml così i test la trovano da './seed'
export { sanitizeHtml } from '../services/prompts';

const VALID_TYPES = ['homepage', 'services', 'zones', 'service_zones', 'blog'] as const;
type PageType = typeof VALID_TYPES[number];

// Interfaccia pagina generata da Gemini — corrisponde a PageContent
interface GeneratedPage {
  slug: string;
  type: string;
  title: string;
  body: string;
  faq: Array<{ question: string; answer: string }>;
  meta: { description: string; canonical: string };
}

// Funzione helper: chiama Gemini con accesso al finishReason raw
// NOTA: non usa AiService.generateContent() perché non espone finishReason
// Replica la stessa logica di AiService ma ritorna l'intero result per validazione
async function callGemini(
  prompt: string,
  env: { GOOGLE_AI_API_KEY: string; CF_ACCOUNT_ID: string; CF_AI_GATEWAY_NAME: string; CF_AI_GATEWAY_TOKEN?: string }
): Promise<GeneratedPage[]> {
  const url = `https://gateway.ai.cloudflare.com/v1/${env.CF_ACCOUNT_ID}/${env.CF_AI_GATEWAY_NAME}/google-ai-studio/v1beta/models/gemini-2.5-flash:generateContent`;

  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    'x-goog-api-key': env.GOOGLE_AI_API_KEY,
  };
  if (env.CF_AI_GATEWAY_TOKEN) {
    headers['Authorization'] = `Bearer ${env.CF_AI_GATEWAY_TOKEN}`;
  }

  const response = await fetch(url, {
    method: 'POST',
    headers,
    body: JSON.stringify({
      contents: [{ parts: [{ text: prompt }] }],
      generationConfig: { responseMimeType: 'application/json' },
    }),
  });

  if (!response.ok) {
    const err = await response.text();
    throw new Error(`AI Gateway error: ${response.status} ${err}`);
  }

  const result = await response.json() as any;

  // CF-04: validare finishReason PRIMA del parse
  const candidate = result.candidates?.[0];
  const finishReason = candidate?.finishReason;
  if (finishReason && finishReason !== 'STOP') {
    throw new Error(`Gemini output incompleto: finishReason=${finishReason}`);
  }

  const textContent: string = candidate?.content?.parts?.[0]?.text ?? '';

  if (!textContent) {
    throw new Error('Gemini ha ritornato risposta vuota');
  }

  // Pitfall 4: regex fallback nel caso Gemini aggiunga testo preamble
  let parsed: any;
  try {
    parsed = JSON.parse(textContent);
  } catch {
    const match = textContent.match(/\[[\s\S]*\]/);
    if (match) {
      parsed = JSON.parse(match[0]);
    } else {
      throw new Error('Impossibile estrarre JSON array dalla risposta Gemini');
    }
  }

  if (!Array.isArray(parsed) || parsed.length === 0) {
    throw new Error('Gemini ha ritornato array vuoto o non-array');
  }

  return parsed as GeneratedPage[];
}

// Funzione helper: scrive batch di pagine su D1 con upsert idempotente (D-04)
async function upsertPages(
  db: ReturnType<typeof drizzle>,
  projectId: string,
  generatedPages: GeneratedPage[]
): Promise<void> {
  const now = new Date().toISOString();
  const rows = generatedPages.map(p => ({
    id: crypto.randomUUID(),
    projectId,
    slug: p.slug,
    type: p.type,
    title: p.title,
    body: sanitizeHtml(p.body),  // D-13: sanitizzazione PRIMA della write
    faq: JSON.stringify(p.faq),
    meta: JSON.stringify(p.meta),
    createdAt: now,
  }));

  await db
    .insert(pages)
    .values(rows)
    .onConflictDoUpdate({
      target: [pages.projectId, pages.slug],
      set: {
        title: sql`excluded.title`,
        body: sql`excluded.body`,
        faq: sql`excluded.faq`,
        meta: sql`excluded.meta`,
      },
    });
}

const api = new Hono<{ Bindings: Bindings }>();

api.post('/seed-project/:projectId', async (c) => {
  const projectId = c.req.param('projectId');
  const type = c.req.query('type') as PageType | undefined;

  // Validazione type — D-02
  if (!type) {
    return c.json({ error: 'Parametro ?type= obbligatorio' }, 400);
  }
  if (!VALID_TYPES.includes(type as any)) {
    return c.json({
      error: `type non valido: "${type}". Valori permessi: ${VALID_TYPES.join(', ')}`,
    }, 400);
  }

  // Lettura body — D-10: services e zones nel request body
  let body: { services?: string[]; zones?: string[]; avatar?: string } = {};
  try {
    body = await c.req.json();
  } catch {
    // body opzionale per alcuni tipi (homepage, blog)
  }

  const avatar = (body.avatar ?? 'in-pain') as AvatarType;
  const services: string[] = body.services ?? [];
  const zones: string[] = body.zones ?? [];

  // Lettura progetto da D1
  const db = drizzle(c.env.DB);
  const project = await db.select().from(projects).where(eq(projects.id, projectId)).get();

  if (!project) {
    return c.json({ error: `Progetto "${projectId}" non trovato` }, 404);
  }

  const context = { niche: project.niche, city: project.location };
  const promptService = new PromptService();
  const geminiEnv = {
    GOOGLE_AI_API_KEY: c.env.GOOGLE_AI_API_KEY,
    CF_ACCOUNT_ID: c.env.CF_ACCOUNT_ID,
    CF_AI_GATEWAY_NAME: c.env.CF_AI_GATEWAY_NAME,
    CF_AI_GATEWAY_TOKEN: c.env.CF_AI_GATEWAY_TOKEN,
  };

  try {
    let totalPages = 0;

    if (type === 'homepage') {
      const prompt = promptService.generateHomepagePrompt(context, avatar);
      const generated = await callGemini(prompt, geminiEnv);
      await upsertPages(db, projectId, generated);
      totalPages = generated.length;

    } else if (type === 'services') {
      if (services.length === 0) {
        return c.json({ error: 'services[] richiesto per type=services' }, 400);
      }
      const prompt = promptService.generateServicesPrompt(context, services, avatar);
      const generated = await callGemini(prompt, geminiEnv);
      await upsertPages(db, projectId, generated);
      totalPages = generated.length;

    } else if (type === 'zones') {
      if (zones.length === 0) {
        return c.json({ error: 'zones[] richiesto per type=zones' }, 400);
      }
      const prompt = promptService.generateZonesPrompt(context, zones, avatar);
      const generated = await callGemini(prompt, geminiEnv);
      await upsertPages(db, projectId, generated);
      totalPages = generated.length;

    } else if (type === 'service_zones') {
      // CF-02 MITIGATION: loop per servizio — N call Gemini, mai una mega-call
      if (services.length === 0 || zones.length === 0) {
        return c.json({ error: 'services[] e zones[] richiesti per type=service_zones' }, 400);
      }
      for (const service of services) {
        const prompt = promptService.generateServiceZonesPrompt(context, service, zones, avatar);
        const generated = await callGemini(prompt, geminiEnv);
        // D-11: write D1 immediatamente dopo ogni call Gemini
        await upsertPages(db, projectId, generated);
        totalPages += generated.length;
      }

    } else if (type === 'blog') {
      const prompt = promptService.generateBlogPrompt(context, avatar);
      const generated = await callGemini(prompt, geminiEnv);
      await upsertPages(db, projectId, generated);
      totalPages = generated.length;
    }

    return c.json({
      success: true,
      type,
      projectId,
      pagesWritten: totalPages,
    });

  } catch (err: any) {
    console.error(`[seed] Error generating type=${type} for project=${projectId}:`, err);
    return c.json({ error: 'Generazione fallita', details: err.message }, 500);
  }
});

export default api;
```

**Aggiornare `factory-core/src/index.ts`:**

Leggere il file attuale per trovare dove è montato `generateApi`. Aggiungere il mount di `seedApi` sullo stesso router `protectedApp`, sulla stessa route base `/api/generate`:

```typescript
import seedApi from './api/seed';
// ...
protectedApp.route('/api/generate', seedApi);
```

In questo modo l'endpoint è raggiungibile come `POST /api/generate/seed-project/:projectId`.

NOTA UUID: Se `uuid` non è in `package.json`, usare `crypto.randomUUID()` (disponibile nei Cloudflare Workers runtime senza import).
  </action>
  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx vitest run src/api/seed.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `factory-core/src/api/seed.ts` esiste e si compila senza errori TypeScript
    - `seed.ts` esporta `sanitizeHtml` (re-export da prompts.ts)
    - `seed.ts` contiene `for (const service of services)` loop per `service_zones`
    - `seed.ts` contiene validazione `finishReason` con throw su non-STOP
    - `seed.ts` contiene regex fallback `text.match(/\[[\s\S]*\]/)` per JSON preamble
    - `seed.ts` usa `onConflictDoUpdate` con target `[pages.projectId, pages.slug]`
    - `seed.ts` applica `sanitizeHtml(p.body)` prima di ogni insert D1
    - `index.ts` monta `seedApi` su `protectedApp`
    - `npx vitest run src/api/seed.test.ts` passa tutti i test (GREEN per FACT-02-a/b/c)
    - `npx wrangler deploy --dry-run` non produce errori TypeScript
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Confine | Descrizione |
|---------|-------------|
| Client → Worker (seed endpoint) | Il request body contiene services[] e zones[] — stringhe usate nel prompt Gemini |
| Worker → Gemini AI Gateway | Prompt costruito da PromptService — include dati project (niche, city) e input services/zones |
| Gemini → D1 | HTML generato da Gemini scritto su D1 dopo sanitizzazione |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Prompt injection via services/zones nel request body | mitigate | services[] e zones[] sono array di stringhe brevi interpolate nel testo del prompt (non HTML, non eseguite). Nessun input utente entra nel body HTML. La struttura JSON del prompt Gemini rende difficile l'injection. Validare che ogni stringa in services/zones sia max 100 caratteri e non contenga newline (aggiungere validazione in seed.ts). |
| T-03-02-02 | Tampering | XSS via body HTML da Gemini scritto su D1 | mitigate | sanitizeHtml() rimuove `<script>`, `<iframe>` e event handler inline prima di ogni write D1. La funzione è applicata in upsertPages() — obbligatoria per ogni tipo. |
| T-03-02-03 | Elevation of Privilege | Auth bypass su /api/generate/seed-project | accept | Endpoint montato su protectedApp — bearerAuth middleware già in place, ereditato automaticamente. Nessun codice auth aggiuntivo necessario. |
| T-03-02-04 | Tampering | Sovrascrittura pagine di altri progetti | mitigate | projectId letto da URL param e verificato con SELECT su D1. Se non esiste → 404. Il WHERE clause in upsertPages usa projectId verificato — impossibile scrivere su altri progetti senza conoscere il loro ID. |
| T-03-02-05 | Denial of Service | Loop service_zones con N molto alto (es. 100 servizi) | accept | Per Phase 3 lo script seed è usato solo dall'operatore autenticato. Wall-clock illimitato su Cloudflare Workers. Scenario DoS non realistico per uso interno. |
</threat_model>

<verification>
Dopo Task 1 (prompts.ts esteso):
```bash
cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx vitest run src/services/prompts.test.ts --reporter=verbose 2>&1 | tail -20
# Expected: tutti i test GREEN — PromptService metodi + sanitizeHtml
```

Dopo Task 2 (seed.ts creato):
```bash
# Test unità
cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx vitest run src/api/seed.test.ts src/services/prompts.test.ts --reporter=verbose 2>&1 | tail -30
# Expected: tutti GREEN

# TypeScript check
cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx wrangler deploy --dry-run 2>&1 | grep -E "error|Error" | grep -v "WARNING" | head -10
# Expected: nessuna riga di errore

# Verifica struttura loop service_zones
grep -n "for.*of.*services" /home/soliwkr/Work/Codice/rankempire-italia/factory-core/src/api/seed.ts
# Expected: riga con 'for (const service of services)'

# Verifica finishReason check
grep -n "finishReason" /home/soliwkr/Work/Codice/rankempire-italia/factory-core/src/api/seed.ts
# Expected: almeno 2 righe (assegnazione + check)

# Verifica upsert
grep -n "onConflictDoUpdate" /home/soliwkr/Work/Codice/rankempire-italia/factory-core/src/api/seed.ts
# Expected: 1 riga nell'helper upsertPages
```
</verification>

<success_criteria>
- `npx vitest run src/api/seed.test.ts src/services/prompts.test.ts` — tutti GREEN
- `prompts.ts` ha 5 nuovi metodi pubblici nella classe PromptService + `sanitizeHtml` esportata
- `seed.ts` ha il loop `for (const service of services)` per type=service_zones
- `seed.ts` valida `finishReason !== 'STOP'` e lancia errore
- `seed.ts` ha regex fallback `text.match(/\[[\s\S]*\]/)` per JSON preamble Gemini
- `seed.ts` applica `sanitizeHtml(p.body)` in `upsertPages()` prima della write D1
- `seed.ts` usa `onConflictDoUpdate` su `(pages.projectId, pages.slug)`
- `index.ts` monta `seedApi` su `protectedApp.route('/api/generate', seedApi)`
- `wrangler deploy --dry-run` termina senza errori TypeScript
</success_criteria>

<output>
Dopo completamento, creare `.planning/phases/03-ai-content-generation/03-02-SUMMARY.md`
</output>
