---
phase: 03-ai-content-generation
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - factory-core/scripts/seed-project.ts
autonomous: false
requirements:
  - FACT-02

user_setup:
  - service: factory-core-local
    why: "Lo script seed-project.ts deve autenticarsi all'endpoint locale con API_SECRET"
    env_vars:
      - name: API_SECRET
        source: "factory-core/.dev.vars — copiare il valore già presente (stesso usato da wrangler dev)"
      - name: GOOGLE_AI_API_KEY
        source: "factory-core/.dev.vars — chiave Gemini per generazione reale"
    dashboard_config:
      - task: "Assicurarsi che wrangler dev sia in esecuzione prima di lanciare lo script"
        location: "Terminale: cd factory-core && npx wrangler dev"

must_haves:
  truths:
    - "Lo script scripts/seed-project.ts esiste e chiama l'endpoint 5 volte in sequenza (una per tipo)"
    - "Lo script legge API_SECRET da .dev.vars per il Bearer token"
    - "Eseguire lo script su un progetto D1 reale produce pagine scritte nel DB locale"
    - "Il DB locale contiene ≥1 homepage, ≥5 service, ≥5 zone, ≥50 service_zone, ≥3 blog per il progetto testato"
    - "L'output dello script mostra il conteggio pagine per ogni tipo e termina con successo"
  artifacts:
    - path: "factory-core/scripts/seed-project.ts"
      provides: "Script orchestratore che chiama l'endpoint 5 volte in sequenza"
      exports: ["default runner"]
    - path: "factory-core/.dev.vars"
      provides: "API_SECRET già presente — il file esiste (non in git)"
  key_links:
    - from: "factory-core/scripts/seed-project.ts"
      to: "http://localhost:8787/api/generate/seed-project/:projectId?type=X"
      via: "fetch con Authorization: Bearer + Content-Type: application/json"
      pattern: "Authorization.*Bearer"
    - from: "D1 locale"
      to: "Contratto PageContent"
      via: "SELECT type, COUNT(*) FROM pages WHERE project_id=? GROUP BY type"
      pattern: "service_zone.*≥50"
---

<objective>
Wave 3 — Script orchestratore e smoke test E2E manuale.

Questo piano crea `scripts/seed-project.ts` che orchestra le 5 chiamate all'endpoint seed in sequenza (D-03). Lo script è il punto di ingresso che l'operatore esegue per popolare un progetto con le ~105 pagine generate.

Dopo la creazione dello script, il piano include un checkpoint di verifica visuale: l'operatore esegue il seed su un progetto di test reale e verifica il conteggio delle pagine nel DB D1 locale.

Purpose: Rendere la Phase 3 completamente operativa — dalla call API alla verifica del conteggio pagine.
Output: scripts/seed-project.ts funzionante + verifica E2E con conteggi reali.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-content-generation/03-CONTEXT.md
@.planning/phases/03-ai-content-generation/03-02-SUMMARY.md

<interfaces>
<!-- Contratti chiave per questo piano. -->

Endpoint da chiamare (creato in Piano 03-02):
```
POST http://localhost:8787/api/generate/seed-project/:projectId?type={tipo}
Authorization: Bearer {API_SECRET}
Content-Type: application/json

Body per type=homepage:
{ "avatar": "in-pain" }

Body per type=services:
{ "services": ["servizio1", "servizio2", ...], "avatar": "in-pain" }

Body per type=zones:
{ "zones": ["Zona1", "Zona2", ...], "avatar": "in-pain" }

Body per type=service_zones:
{ "services": ["servizio1", "servizio2", ...], "zones": ["Zona1", "Zona2", ...], "avatar": "in-pain" }

Body per type=blog:
{ "avatar": "in-pain" }

Response 200:
{ "success": true, "type": "...", "projectId": "...", "pagesWritten": N }

Response 400/404/500:
{ "error": "...", "details": "..." }
```

Da factory-core/scripts/test-ai-real.ts (pattern lettura .dev.vars da replicare):
```typescript
import fs from 'fs';
import path from 'path';

const devVarsPath = path.join(__dirname, '../.dev.vars');
const devVarsRaw = fs.readFileSync(devVarsPath, 'utf-8');
const env: Record<string, string> = {};

devVarsRaw.split('\n').forEach(line => {
  const [key, ...value] = line.split('=');
  if (key && value) env[key.trim()] = value.join('=').trim();
});
// Usa: env.API_SECRET, env.FACTORY_URL
```

Verifica D1 post-seed:
```bash
cd factory-core && npx wrangler d1 execute factory-db --local \
  --command "SELECT type, COUNT(*) as count FROM pages WHERE project_id='PROJ_ID' GROUP BY type ORDER BY type;"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare scripts/seed-project.ts — orchestratore 5 chiamate sequenziali</name>
  <files>factory-core/scripts/seed-project.ts</files>
  <read_first>
    - factory-core/scripts/test-ai-real.ts — pattern lettura .dev.vars (leggere TUTTO — la struttura è identica)
    - factory-core/.dev.vars — verificare che API_SECRET sia presente (non leggere il valore, solo la chiave)
    - .planning/phases/03-ai-content-generation/03-CONTEXT.md §D-03 — comportamento atteso dello script
  </read_first>
  <action>
Creare `factory-core/scripts/seed-project.ts`.

Lo script accetta un `projectId` come primo argomento CLI e un `avatar` opzionale come secondo (default: `in-pain`).

**Struttura completa dello script:**

```typescript
#!/usr/bin/env node
/**
 * seed-project.ts — Phase 3 orchestratore
 *
 * Uso: npx tsx scripts/seed-project.ts <projectId> [avatar] [servizi...] [zone...]
 *
 * Esempi:
 *   npx tsx scripts/seed-project.ts proj-formia-idraulico
 *   npx tsx scripts/seed-project.ts proj-formia-idraulico in-pain
 *
 * Richiede:
 *   - .dev.vars con API_SECRET e FACTORY_URL (default: http://localhost:8787)
 *   - wrangler dev in esecuzione: cd factory-core && npx wrangler dev
 *   - Progetto esistente in D1 con niche e location popolati
 *
 * Configurazione servizi/zone: modificare le costanti SERVICES e ZONES sotto
 * oppure passarle come argomenti JSON (terzo e quarto argomento).
 */

import fs from 'fs';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));

// --- Lettura .dev.vars (pattern da test-ai-real.ts) ---
const devVarsPath = path.join(__dirname, '../.dev.vars');
let env: Record<string, string> = {};
try {
  const devVarsRaw = fs.readFileSync(devVarsPath, 'utf-8');
  devVarsRaw.split('\n').forEach(line => {
    const [key, ...value] = line.split('=');
    if (key && key.trim() && value.length > 0) {
      env[key.trim()] = value.join('=').trim();
    }
  });
} catch {
  console.error('ERRORE: .dev.vars non trovato. Creare factory-core/.dev.vars con API_SECRET.');
  process.exit(1);
}

const BASE_URL = env.FACTORY_URL || 'http://localhost:8787';
const API_SECRET = env.API_SECRET;

if (!API_SECRET) {
  console.error('ERRORE: API_SECRET non trovato in .dev.vars');
  process.exit(1);
}

// --- Argomenti CLI ---
const args = process.argv.slice(2);
const projectId = args[0];
const avatar = (args[1] as 'in-pain' | 'skeptic' | 'bundler') || 'in-pain';

if (!projectId) {
  console.error('ERRORE: projectId richiesto come primo argomento');
  console.error('Uso: npx tsx scripts/seed-project.ts <projectId> [avatar]');
  process.exit(1);
}

// --- Configurazione servizi e zone ---
// MODIFICA QUESTI VALORI per il progetto specifico
// Oppure popola project.configJson in D1 e leggi da lì in future iterazioni
const SERVICES = [
  'riparazioni-perdite',
  'installazione-caldaie',
  'installazione-sanitari',
  'manutenzione-impianti',
  'pronto-intervento',
];

const ZONES = [
  'Formia',
  'Gaeta',
  'Minturno',
  'Cassino',
  'Terracina',
];

// --- Helper: chiamata autenticata all'endpoint seed ---
async function seedType(type: string, body: object): Promise<{ pagesWritten: number }> {
  const url = `${BASE_URL}/api/generate/seed-project/${projectId}?type=${type}`;
  console.log(`\n[seed] ${type} → POST ${url}`);

  const res = await fetch(url, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${API_SECRET}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(body),
  });

  if (!res.ok) {
    const text = await res.text();
    throw new Error(`${type} fallito: HTTP ${res.status} — ${text}`);
  }

  const data = await res.json() as any;
  console.log(`[seed] ${type} → ${data.pagesWritten} pagine scritte ✓`);
  return { pagesWritten: data.pagesWritten };
}

// --- Esecuzione sequenziale 5 tipi (D-03) ---
async function main() {
  console.log(`\n=== SEED PROJECT: ${projectId} (avatar: ${avatar}) ===`);
  console.log(`Target: ${BASE_URL}`);
  console.log(`Servizi: ${SERVICES.join(', ')}`);
  console.log(`Zone: ${ZONES.join(', ')}`);
  console.log('');

  let totalPages = 0;

  try {
    // 1. homepage — 1 pagina
    const hp = await seedType('homepage', { avatar });
    totalPages += hp.pagesWritten;

    // 2. services — N pagine (una per servizio)
    const sv = await seedType('services', { services: SERVICES, avatar });
    totalPages += sv.pagesWritten;

    // 3. zones — N pagine (una per zona)
    const zn = await seedType('zones', { zones: ZONES, avatar });
    totalPages += zn.pagesWritten;

    // 4. service_zones — N×M pagine (loop interno nel Worker per CF-02)
    // Lo script chiama una sola volta; il Worker fa il loop per servizio
    const sz = await seedType('service_zones', { services: SERVICES, zones: ZONES, avatar });
    totalPages += sz.pagesWritten;

    // 5. blog — K pagine (Gemini decide, floor ≥3)
    const bl = await seedType('blog', { avatar });
    totalPages += bl.pagesWritten;

    console.log(`\n=== COMPLETATO: ${totalPages} pagine totali scritte su D1 ===`);
    console.log(`\nVerifica conteggio:\n  cd factory-core && npx wrangler d1 execute factory-db --local --command "SELECT type, COUNT(*) as count FROM pages WHERE project_id='${projectId}' GROUP BY type ORDER BY type;"`);

  } catch (err: any) {
    console.error(`\n[seed] ERRORE: ${err.message}`);
    console.error('Assicurarsi che wrangler dev sia in esecuzione: cd factory-core && npx wrangler dev');
    process.exit(1);
  }
}

main();
```

**Pitfall 5 (da RESEARCH.md):** Lo script legge `API_SECRET` da `.dev.vars`, non da variabili d'ambiente del processo. Il commento nel file documenta questo requisito esplicitamente.
  </action>
  <verify>
    <automated>ls /home/soliwkr/Work/Codice/rankempire-italia/factory-core/scripts/seed-project.ts && grep -c "seedType" /home/soliwkr/Work/Codice/rankempire-italia/factory-core/scripts/seed-project.ts</automated>
  </verify>
  <done>
    - `factory-core/scripts/seed-project.ts` esiste
    - File contiene 5 chiamate a `seedType` per i 5 tipi in sequenza
    - File legge API_SECRET da `.dev.vars` (stesso pattern di test-ai-real.ts)
    - File accetta projectId come argomento CLI (process.argv[2])
    - File stampa conteggio pagine per ogni tipo + comando verifica D1
    - `npx tsx scripts/seed-project.ts --help` non lancia syntax error (controllo sintattico)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test E2E — eseguire seed su progetto di test + verificare conteggi D1</name>
  <what-built>
Lo script `scripts/seed-project.ts` e l'endpoint `POST /api/generate/seed-project/:projectId?type=X` sono stati creati nei piani precedenti. Questo checkpoint verifica che il sistema funzioni end-to-end con una chiamata Gemini reale che produce pagine scritte su D1.
  </what-built>
  <how-to-verify>
**Pre-requisiti (verificare prima di procedere):**

1. Trovare un progetto esistente in D1 locale:
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
   npx wrangler d1 execute factory-db --local \
     --command "SELECT id, niche, location FROM projects LIMIT 5;"
   ```
   Annotare un `id` valido (esempio: `proj-formia-idraulico`).

2. Avviare wrangler dev in un terminale separato (lasciare in background):
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
   npx wrangler dev
   ```
   Attendere "Ready on http://localhost:8787".

3. Verificare che `.dev.vars` contenga `API_SECRET` e `GOOGLE_AI_API_KEY`:
   ```bash
   grep -c "API_SECRET\|GOOGLE_AI_API_KEY" /home/soliwkr/Work/Codice/rankempire-italia/factory-core/.dev.vars
   ```
   Expected: 2 (o più se ci sono altri valori).

**Esecuzione seed:**

4. Modificare `SERVICES` e `ZONES` in `seed-project.ts` per corrispondere al progetto trovato al passo 1 (es. per un idraulico a Formia: services = ['riparazioni', 'caldaie', ...], zones = ['Formia', 'Gaeta', ...]).

5. Eseguire lo script:
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
   npx tsx scripts/seed-project.ts {PROJECT_ID} in-pain
   ```
   (Sostituire {PROJECT_ID} con l'id trovato al passo 1)

   Lo script può impiegare 2-5 minuti per completare le 5 chiamate Gemini (service_zones è la più lenta — N call sequenziali).

**Verifica conteggi pagine:**

6. Dopo che lo script termina con "COMPLETATO: X pagine totali scritte su D1":
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
   npx wrangler d1 execute factory-db --local \
     --command "SELECT type, COUNT(*) as count FROM pages WHERE project_id='{PROJECT_ID}' GROUP BY type ORDER BY type;"
   ```

   **Conteggi attesi (Success Criteria da ROADMAP Phase 3):**
   - `blog`: ≥ 3
   - `homepage`: = 1
   - `service`: ≥ 5
   - `service_zone`: ≥ 25 (5 servizi × 5 zone minimo)
   - `zone`: ≥ 5

7. Ispezionare una pagina service_zone per verifica qualità:
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
   npx wrangler d1 execute factory-db --local \
     --command "SELECT slug, title, substr(body, 1, 200) as body_preview FROM pages WHERE project_id='{PROJECT_ID}' AND type='service_zone' LIMIT 3;"
   ```
   
   Verificare visivamente che:
   - Il titolo contiene il nome del servizio e della zona in italiano
   - Il body preview è testo HTML italiano (non inglese, non placeholder)
   - Il body non contiene `<script>` o `onclick=`

8. Rieseguire lo script una seconda volta per verificare l'idempotenza (D-04):
   ```bash
   npx tsx scripts/seed-project.ts {PROJECT_ID} in-pain
   ```
   Il conteggio totale nel DB deve rimanere uguale (upsert, non insert duplicati).
  </how-to-verify>
  <resume-signal>
Scrivere "approvato" se:
- Lo script ha completato senza errori
- I conteggi D1 rispettano i minimi (homepage=1, service≥5, zone≥5, service_zone≥25, blog≥3)
- Il body è in italiano naturale senza script tags
- Il secondo run non ha aumentato il conteggio (idempotenza confermata)

Scrivere "fallito: [descrizione problema]" se qualcosa non funziona (es. "fallito: finishReason MAX_TOKENS su service_zones").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Confine | Descrizione |
|---------|-------------|
| Script Node.js → Worker locale | Lo script legge API_SECRET da .dev.vars in chiaro — solo ambiente di sviluppo locale |
| Worker → D1 locale | Pagine generate scritte sul DB SQLite locale (.wrangler/) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | API_SECRET in .dev.vars letto da script | accept | .dev.vars è già in .gitignore (pattern Cloudflare standard). Solo ambiente locale. Il SECRET è lo stesso usato da wrangler dev — nessuna superficie aggiuntiva. |
| T-03-03-02 | Tampering | SERVICES e ZONES hardcoded nello script | accept | Per Phase 3 lo script è usato solo dall'operatore — valori hardcoded sono documentati come da modificare per ogni progetto. Phase 4 leggerà da configJson su D1. |
</threat_model>

<verification>
```bash
# Verifica struttura script
ls /home/soliwkr/Work/Codice/rankempire-italia/factory-core/scripts/seed-project.ts
grep -n "seedType\|homepage\|services\|zones\|service_zones\|blog" /home/soliwkr/Work/Codice/rankempire-italia/factory-core/scripts/seed-project.ts | head -20

# Verifica sintassi TypeScript (dry-run)
cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx wrangler deploy --dry-run 2>&1 | grep -E "^error" | head -5

# Verifica test suite completa (tutti e 3 i piani)
cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx vitest run src/ --reporter=verbose 2>&1 | tail -20
```
</verification>

<success_criteria>
- `factory-core/scripts/seed-project.ts` esiste con 5 chiamate seedType in sequenza
- Lo script legge API_SECRET da `.dev.vars`
- Il checkpoint visivo conferma: ≥105 pagine totali scritte (homepage=1, service≥5, zone≥5, service_zone≥25, blog≥3)
- Il body delle pagine è in italiano naturale senza `<script>` o event handler
- Rieseguire lo script non aumenta il conteggio pagine (idempotenza D-04 confermata)
- `npx vitest run src/` — tutta la suite GREEN
</success_criteria>

<output>
Dopo completamento e approvazione del checkpoint, creare `.planning/phases/03-ai-content-generation/03-03-SUMMARY.md`
</output>
