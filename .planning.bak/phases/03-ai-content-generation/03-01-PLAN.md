---
phase: 03-ai-content-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - factory-core/src/db/schema.ts
  - factory-core/src/api/seed.test.ts
  - factory-core/src/services/prompts.test.ts
autonomous: true
requirements:
  - FACT-02

must_haves:
  truths:
    - "La tabella pages ha un unique index su (project_id, slug) applicato al DB locale"
    - "onConflictDoUpdate su (projectId, slug) non lancia errore SQLite"
    - "sanitizeHtml rimuove <script>, <iframe> e handler inline da qualsiasi stringa HTML"
    - "I test RED di seed.test.ts e prompts.test.ts esistono e falliscono per mancanza dell'implementazione, non per errori di sintassi"
  artifacts:
    - path: "factory-core/src/db/schema.ts"
      provides: "uniqueIndex pages_project_slug_uniq su (project_id, slug)"
      contains: "pages_project_slug_uniq"
    - path: "factory-core/src/api/seed.test.ts"
      provides: "Test stub RED per FACT-02-a/b/c/f"
      exports: ["describe POST /api/generate/seed-project/:projectId"]
    - path: "factory-core/src/services/prompts.test.ts"
      provides: "Test stub RED per FACT-02-d/e"
      exports: ["describe PromptService", "describe sanitizeHtml"]
  key_links:
    - from: "factory-core/src/db/schema.ts"
      to: "D1 locale (.wrangler/)"
      via: "wrangler d1 execute --local --command CREATE UNIQUE INDEX"
      pattern: "pages_project_slug_uniq"
    - from: "factory-core/src/api/seed.test.ts"
      to: "factory-core/src/api/seed.ts"
      via: "import api from './seed'"
      pattern: "import api from './seed'"
---

<objective>
Wave 0 — fondamenta bloccanti per la Phase 3.

Questo piano fa tre cose in sequenza obbligatoria:
1. Aggiunge il `uniqueIndex` su `pages(project_id, slug)` in schema.ts [BLOCKING per D-04]
2. Applica l'indice al DB D1 locale tramite raw SQL (non drizzle-kit, che richiede conferma interattiva)
3. Crea i test stub RED per seed.test.ts e prompts.test.ts — i test devono esistere e fallire prima che l'implementazione esista (Nyquist compliance)

Il Piano 03-02 non può partire senza che il DB locale abbia l'indice univoco e i test stub esistano.

Purpose: Garantire che l'upsert D1 e la validazione siano testabili prima dell'implementazione.
Output: schema.ts aggiornato + DB locale migrato + 2 file test stub RED.
</objective>

<execution_context>
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/workflows/execute-plan.md
@/home/soliwkr/Work/Codice/rankempire-italia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-content-generation/03-CONTEXT.md
@.planning/phases/03-ai-content-generation/03-RESEARCH.md

<interfaces>
<!-- Contratti chiave che questo piano usa e modifica. -->

Da factory-core/src/db/schema.ts (stato attuale — LEGGERE prima di modificare):
```typescript
import { sqliteTable, text, integer, real } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';

export const pages = sqliteTable('pages', {
  id: text('id').primaryKey(),
  projectId: text('project_id').notNull().references(() => projects.id),
  slug: text('slug').notNull(),
  type: text('type').notNull(), // 'homepage'|'service'|'zone'|'service_zone'|'blog'
  title: text('title').notNull(),
  body: text('body').notNull(),
  faq: text('faq').notNull().default('[]'),
  meta: text('meta').notNull().default('{}'),
  createdAt: text('created_at').default(sql`CURRENT_TIMESTAMP`).notNull(),
});
// MANCA: uniqueIndex su (project_id, slug) — da aggiungere in Task 1
```

Da factory-core/src/api/leads.test.ts (pattern test da replicare esattamente):
```typescript
import { describe, it, expect } from 'vitest';
import api from './leads';

describe('POST /api/leads', () => {
  it('should reject invalid data', async () => {
    const res = await api.request('/', {
      method: 'POST',
      body: JSON.stringify({ name: 'J' }),
    }, { DB: {} as any });
    expect(res.status).toBe(400);
  });
});
```

Da factory-core/src/services/prompts.ts (classe da estendere in Piano 03-02):
```typescript
export type AvatarType = 'in-pain' | 'skeptic' | 'bundler';
export class PromptService {
  generatePrompt(context: ProjectContext, avatar: AvatarType): string { ... }
  // I 5 nuovi metodi verranno aggiunti in Piano 03-02
  // generateHomepagePrompt, generateServicesPrompt, generateZonesPrompt,
  // generateServiceZonesPrompt, generateBlogPrompt
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggiungere uniqueIndex a schema.ts + applicare al DB locale [BLOCKING]</name>
  <files>factory-core/src/db/schema.ts</files>
  <read_first>
    - factory-core/src/db/schema.ts — stato attuale completo (leggere TUTTO prima di modificare)
    - factory-core/.wrangler/ — verificare che la directory esista (conferma DB locale inizializzato)
  </read_first>
  <behavior>
    - Test 1: Dopo la modifica, `grep "pages_project_slug_uniq" factory-core/src/db/schema.ts` ritorna un match
    - Test 2: Dopo il push SQL, `npx wrangler d1 execute factory-db --local --command "SELECT name FROM sqlite_master WHERE type='index' AND name='pages_project_slug_uniq';"` ritorna 1 riga
    - Test 3: Build TypeScript (dry-run wrangler) non produce errori di compilazione
  </behavior>
  <action>
Modificare `factory-core/src/db/schema.ts`:

1. Aggiungere `uniqueIndex` agli import da `drizzle-orm/sqlite-core`:
   ```typescript
   import { sqliteTable, text, integer, real, uniqueIndex } from 'drizzle-orm/sqlite-core';
   ```

2. Modificare la definizione di `pages` per includere il secondo argomento `(table) => (...)`:
   ```typescript
   export const pages = sqliteTable('pages', {
     id: text('id').primaryKey(),
     projectId: text('project_id').notNull().references(() => projects.id),
     slug: text('slug').notNull(),
     type: text('type').notNull(), // 'homepage'|'service'|'zone'|'service_zone'|'blog'
     title: text('title').notNull(),
     body: text('body').notNull(),
     faq: text('faq').notNull().default('[]'),   // JSON string: [{ question, answer }]
     meta: text('meta').notNull().default('{}'), // JSON string: { description, canonical }
     createdAt: text('created_at').default(sql`CURRENT_TIMESTAMP`).notNull(),
   }, (table) => ({
     projectSlugUniq: uniqueIndex('pages_project_slug_uniq').on(table.projectId, table.slug),
   }));
   ```

3. Applicare l'indice al DB D1 locale con raw SQL (non drizzle-kit push — richiede TTY interattivo):
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
   npx wrangler d1 execute factory-db --local --command "CREATE UNIQUE INDEX IF NOT EXISTS pages_project_slug_uniq ON pages(project_id, slug);"
   ```

4. Verificare TypeScript clean:
   ```bash
   cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core
   npx wrangler deploy --dry-run 2>&1 | grep -E "error|Error" | head -10
   ```
   Se nessuna riga di errore → OK.
  </action>
  <verify>
    <automated>grep "pages_project_slug_uniq" /home/soliwkr/Work/Codice/rankempire-italia/factory-core/src/db/schema.ts && cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx wrangler d1 execute factory-db --local --command "SELECT name FROM sqlite_master WHERE type='index' AND name='pages_project_slug_uniq';" 2>&1 | grep -c "pages_project_slug_uniq"</automated>
  </verify>
  <done>
    - `schema.ts` contiene `uniqueIndex('pages_project_slug_uniq').on(table.projectId, table.slug)`
    - Il DB D1 locale ha l'indice applicato (query sqlite_master ritorna 1 riga)
    - `wrangler deploy --dry-run` termina senza errori TypeScript
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Creare test stub RED — seed.test.ts e prompts.test.ts</name>
  <files>
    factory-core/src/api/seed.test.ts
    factory-core/src/services/prompts.test.ts
  </files>
  <read_first>
    - factory-core/src/api/leads.test.ts — pattern esatto da replicare (struttura import, api.request(), terzo argomento Bindings mock)
    - factory-core/src/services/prompts.ts — AvatarType, classe PromptService (vedere i metodi esistenti — i nuovi metodi non esistono ancora)
  </read_first>
  <behavior>
    - Test 1: `npx vitest run src/api/seed.test.ts` fallisce con "Cannot find module './seed'" o simile — NON con errori di sintassi TypeScript
    - Test 2: `npx vitest run src/services/prompts.test.ts` fallisce con "service.generateHomepagePrompt is not a function" o simile
    - Test 3: Nessun test in questi file produce "syntax error" o "unexpected token"
  </behavior>
  <action>
Creare `factory-core/src/api/seed.test.ts` — test stub RED che coprono FACT-02-a/b/c/f.
Il file importa `./seed` che non esiste ancora — il test fallirà per import mancante (RED corretto):

```typescript
import { describe, it, expect } from 'vitest';
import api from './seed';

describe('POST /api/generate/seed-project/:projectId', () => {
  it('FACT-02-a: dovrebbe ritornare 400 se type è mancante', async () => {
    const res = await api.request('/seed-project/proj-test-1', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ services: ['idraulico'], zones: ['Formia'], avatar: 'in-pain' }),
    }, { DB: {} as any, GOOGLE_AI_API_KEY: 'test', CF_ACCOUNT_ID: 'test', CF_AI_GATEWAY_NAME: 'test', CF_AI_GATEWAY_TOKEN: 'test' } as any);
    expect(res.status).toBe(400);
  });

  it('FACT-02-b: dovrebbe ritornare 400 se type è invalido', async () => {
    const res = await api.request('/seed-project/proj-test-1?type=invalid_type', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ services: ['idraulico'], zones: ['Formia'], avatar: 'in-pain' }),
    }, { DB: {} as any, GOOGLE_AI_API_KEY: 'test', CF_ACCOUNT_ID: 'test', CF_AI_GATEWAY_NAME: 'test', CF_AI_GATEWAY_TOKEN: 'test' } as any);
    expect(res.status).toBe(400);
  });

  it('FACT-02-c: dovrebbe ritornare 404 se projectId non esiste in D1', async () => {
    // DB mock: select().from().where().get() ritorna null
    const mockDb = {
      prepare: () => ({ bind: () => ({ first: async () => null }) }),
    };
    const res = await api.request('/seed-project/progetto-inesistente?type=homepage', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ services: [], zones: [], avatar: 'in-pain' }),
    }, { DB: mockDb as any, GOOGLE_AI_API_KEY: 'test', CF_ACCOUNT_ID: 'test', CF_AI_GATEWAY_NAME: 'test', CF_AI_GATEWAY_TOKEN: 'test' } as any);
    expect(res.status).toBe(404);
  });

  it('FACT-02-f: dovrebbe ritornare 500 se finishReason non è STOP', async () => {
    // Questo test verrà completato in Piano 03-02 con un mock AiService
    // Per ora verifica che il tipo del body sia corretto (validazione input passa)
    // Il mock Gemini che ritorna finishReason=MAX_TOKENS è fuori scope del test stub
    expect(true).toBe(true); // placeholder — sarà sostituito in Piano 03-02
  });
});
```

Creare `factory-core/src/services/prompts.test.ts` — test stub RED che coprono FACT-02-d/e:

```typescript
import { describe, it, expect } from 'vitest';
import { PromptService } from './prompts';
// sanitizeHtml sarà esportata da factory-core/src/api/seed.ts (creato in Piano 03-02)
// Per ora importiamo in modo da ottenere un RED sulla import mancante
import { sanitizeHtml } from '../api/seed';

describe('PromptService — nuovi metodi Phase 3', () => {
  const service = new PromptService();

  it('FACT-02-d: generateHomepagePrompt include avatar in-pain', () => {
    const prompt = (service as any).generateHomepagePrompt(
      { niche: 'idraulico', city: 'Formia' },
      'in-pain'
    );
    expect(typeof prompt).toBe('string');
    expect(prompt.length).toBeGreaterThan(100);
    expect(prompt).toContain('IN-PAIN');
    expect(prompt).toContain('Formia');
  });

  it('FACT-02-d: generateServicesPrompt include ogni servizio passato', () => {
    const prompt = (service as any).generateServicesPrompt(
      { niche: 'idraulico', city: 'Formia' },
      ['riparazioni', 'installazioni'],
      'skeptic'
    );
    expect(prompt).toContain('riparazioni');
    expect(prompt).toContain('installazioni');
  });

  it('FACT-02-d: generateServiceZonesPrompt include servizio e zone', () => {
    const prompt = (service as any).generateServiceZonesPrompt(
      { niche: 'idraulico', city: 'Formia' },
      'riparazioni',
      ['Gaeta', 'Minturno'],
      'bundler'
    );
    expect(prompt).toContain('riparazioni');
    expect(prompt).toContain('Gaeta');
    expect(prompt).toContain('Minturno');
  });

  it('FACT-02-d: generateBlogPrompt include niche e city', () => {
    const prompt = (service as any).generateBlogPrompt(
      { niche: 'idraulico', city: 'Formia' },
      'in-pain'
    );
    expect(prompt).toContain('idraulico');
    expect(prompt).toContain('Formia');
  });
});

describe('sanitizeHtml', () => {
  it('FACT-02-e: rimuove tag script', () => {
    const input = '<p>Testo OK</p><script>alert(1)</script>';
    const output = sanitizeHtml(input);
    expect(output).not.toContain('<script>');
    expect(output).toContain('<p>Testo OK</p>');
  });

  it('FACT-02-e: rimuove tag iframe', () => {
    const input = '<p>OK</p><iframe src="evil.com"></iframe>';
    const output = sanitizeHtml(input);
    expect(output).not.toContain('<iframe');
  });

  it('FACT-02-e: rimuove event handler inline', () => {
    const input = '<p onclick="evil()">Click me</p>';
    const output = sanitizeHtml(input);
    expect(output).not.toContain('onclick=');
  });
});
```
  </action>
  <verify>
    <automated>cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx vitest run src/api/seed.test.ts src/services/prompts.test.ts --reporter=verbose 2>&1 | grep -E "FAIL|Cannot find module|is not a function" | head -10</automated>
  </verify>
  <done>
    - `seed.test.ts` esiste e fallisce con "Cannot find module './seed'" (RED atteso)
    - `prompts.test.ts` esiste e fallisce per metodi non ancora implementati (RED atteso)
    - Nessun file ha errori di sintassi TypeScript (solo import mancanti)
    - I 2 file sono verificabili con `npx vitest run src/api/seed.test.ts src/services/prompts.test.ts`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Confine | Descrizione |
|---------|-------------|
| schema.ts → D1 locale | Modifica schema applicata via raw SQL a DB SQLite locale (.wrangler/) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | schema.ts unique index | accept | Rischio minimo: modifica idempotente (IF NOT EXISTS nella SQL raw), nessun dato utente coinvolto |
| T-03-01-02 | Tampering | test stub con mock DB | accept | I test usano `DB: {} as any` — mock intenzionalmente permissivo per unit test; il vero auth è sul Worker runtime |
</threat_model>

<verification>
Dopo Task 1:
```bash
grep "pages_project_slug_uniq" /home/soliwkr/Work/Codice/rankempire-italia/factory-core/src/db/schema.ts
# Expected: riga con uniqueIndex('pages_project_slug_uniq').on(table.projectId, table.slug)

cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx wrangler d1 execute factory-db --local --command "SELECT name FROM sqlite_master WHERE type='index' AND name='pages_project_slug_uniq';" 2>&1 | grep "pages_project_slug_uniq"
# Expected: 1 riga con nome indice
```

Dopo Task 2:
```bash
cd /home/soliwkr/Work/Codice/rankempire-italia/factory-core && npx vitest run src/api/seed.test.ts src/services/prompts.test.ts --reporter=verbose 2>&1 | tail -20
# Expected: test falliscono per import mancante, NON per syntax error
```
</verification>

<success_criteria>
- schema.ts ha `uniqueIndex('pages_project_slug_uniq').on(table.projectId, table.slug)` nella definizione di `pages`
- DB D1 locale ha l'indice `pages_project_slug_uniq` applicato
- `factory-core/src/api/seed.test.ts` esiste con 4 test case (FACT-02-a/b/c/f)
- `factory-core/src/services/prompts.test.ts` esiste con test per i 5 metodi PromptService e 3 test sanitizeHtml
- I test sono in stato RED (falliscono per implementazione mancante, non per syntax error)
- `wrangler deploy --dry-run` non produce errori TypeScript
</success_criteria>

<output>
Dopo completamento, creare `.planning/phases/03-ai-content-generation/03-01-SUMMARY.md`
</output>
